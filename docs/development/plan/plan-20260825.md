# `libra code` provider 解析与凭据文案收口计划（2026-08-25）

> 模板版本: v2.1（按 [`plan-template.md`](plan-template.md) 成稿；本计划为新建计划，整份符合 v2.1）
> 触发: 用户报告 `libra code` 无参启动直接 `fatal: GEMINI_API_KEY is not set; export GEMINI_API_KEY or store it with ...` —— 且提示中给出的 `libra config --global add vault.env.GEMINI_API_KEY <value>` 实测不可执行（`config` 无 `add` 子命令，报 `LBR-CLI-002`）
> 用户裁决: **不考虑存量兼容**（2026-08-24 明确指示），破坏性行为变更以 `minor` 承载
> 文案口径: 全部用户可见文案统一采用 **ADR-PS-03「a 模式」**

## 文档职责

本文解决 `libra code` 的 provider 选择与凭据缺失反馈问题，目标是：**无参启动要么成功并说明用了谁，要么给出可直接复制执行的下一步**，并消除「生效 provider 与 UI/计费标签分裂」这一既有缺陷。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `src/command/code.rs` 的 `--provider` 参数面、入口解析与写回。
- `src/internal/config.rs` 的凭据定位（新增「命中层」信息），以及新配置键 `code.defaultProvider`。
- `libra code` 三类用户可见文案：零凭据、唯一凭据、多凭据。
- 生效 provider 的单一事实源收口（banner / runtime kind / plan-mode / system preamble / usage 计费标签）。
- 会话元数据持久化 provider/model 与 `--resume` 继承。
- `docs/commands/code.md` EN + zh 的凭据优先级订正与新行为文档化。

### 非目标

- 不改 `src/internal/ai/providers/factory.rs` 的任何 provider 构造语义（只消费其 `MissingApiKey { env_var }`）。
- 不新增 provider、不改各 provider 的默认模型常量、不动 `--agent` profile 的绑定语义。
- 不做交互式 provider 选择器（TUI/提示符选单）；本计划只做非交互的判定与文案。
- 不新增 `StableErrorCode`（ADR-PS-04）。
- 不改 `--stdio`（MCP legacy）与 `--control stdio`（client shim）的能力面，只保证它们在零凭据下仍可启动。
- 不实现 provider 健康探活（网络握手 / key 有效性校验）——本计划只判断「是否已配置」。

### 成功定义

- 无参 `libra code` 在恰好配置一个凭据时直接启动，并在 stderr 说明选了谁、凭据来自哪一层。
- 零凭据时的报错含**可直接复制执行**的 `libra config set --global …`，以及免凭据的 `--provider codex` / 本地 ollama 路径。
- 多凭据时报可判定的歧义错误，列出全部候选（provider + 变量名 + 命中层），且**不含任何 key 值或片段**。
- 显式 `--provider` 时探测完全不介入；`--stdio` / `--control stdio` 在零凭据下仍可启动。
- `libra code` 打印的 provider、system preamble、usage 记录与实际驱动会话的 provider 完全一致（含 `--agent` 覆盖路径）。
- `--resume` 能从会话元数据继承 provider/model；显式冲突时告警而非静默换模型。
- 所有非延后任务 `Lifecycle=done` 且 `Acceptance=complete`；计划完成门全绿。

## 事实基线

> 锚点核对日期：**2026-08-24**。crate `0.21.23`。开工时按 ER-02 刷新行号。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 参数默认 | `--provider` 为 `default_value_t = CodeProvider::Gemini`，无参启动即解析 `GEMINI_API_KEY` | `src/command/code.rs:519-521`；`src/command/code.rs:1808` |
| 凭据链 | 实际优先级 = `--env-file` → 进程 env → repo vault → global vault | `src/command/code.rs:1575-1584`；`src/internal/config.rs:1152-1165` |
| 缺 key 文案 | 提示 `libra config --global add vault.env.<KEY> <value>`；`config` 无 `add` 子命令，实测 `LBR-CLI-002`（`config set --global` / `config --global --add` 才成立） | `src/command/code.rs:1880-1890`；`src/command/config.rs:375-393` |
| 显式/默认不可分 | `--stdio` 靠 `args.provider != CodeProvider::Gemini` 反推「用户是否显式传入」；全仓无 clap 显式性 API 用法 | `src/command/code.rs:4446`；`rg -n 'value_source\|ArgMatches' src/` → 0 命中（2026-08-24） |
| 早于工厂的消费点 | plan-mode 默认、web 运行时分派、banner、provider match、preamble、usage 标签 | `src/command/code.rs:682`、`src/command/code.rs:1207`、`src/command/code.rs:1272`、`src/command/code.rs:2961`、`src/command/code.rs:2824`、`src/command/code.rs:2732`、`src/command/code.rs:2845` |
| 工厂返回值被丢弃 | 两处 `let (model, model_name, _) = build_any_completion_model_for_args(...)`，`--agent` 覆盖后标签仍取 `args.provider` | `src/command/code.rs:2969`、`src/command/code.rs:2996` |
| 免凭据路径 | `codex` 无 API Key 列；`ollama` 仅 Cloud base URL 需要 key，且缺 `--model` 直接 usage 错 | `docs/commands/code.md:69-77`；`src/internal/ai/providers/factory.rs:285-300`；`src/command/code.rs:1774-1779` |
| 会话元数据 | live 路径从不写入 `provider`（仅 store 单测手塞）；`--resume` 无从继承 | `rg -n '"provider"' src/command/code.rs src/internal/ai/web/code_ui.rs src/internal/ai/web/code_ui_projection.rs` → 0 命中（2026-08-24）；`src/internal/ai/session/store.rs:1254` 与 `src/internal/ai/session/store.rs:1270` 均在 `mod tests`（`mod tests` 起于 `src/internal/ai/session/store.rs:879`）；`src/command/code.rs:2380-2410` |
| 错误码 | `LBR-AUTH-001` = 缺凭据（exit 128）；`LBR-CLI-002` = 参数无效/缺失（exit 129） | `docs/error-codes.md:107`、`docs/error-codes.md:88`；`src/utils/error.rs:805-808`、`src/utils/error.rs:831-833` |
| 配置级联 | `read_cascaded_config_value(local → global)` 已存在，可直接承载 `code.defaultProvider` | `src/internal/config.rs:1242-1250` |
| 同步解析成本 | `resolve_env_sync` 在进程 env 未命中时每次 spawn std::thread + 新建 tokio Runtime + 开 SQLite | `src/internal/config.rs:1100-1122` |
| 文档漂移 | EN/zh 第 82 行写「repo-local Vault → global Vault → 进程环境」，与代码实际顺序相反 | `docs/commands/code.md:82`；`docs/commands/zh-CN/code.md:82` |
| 版本面 | 五处均为 `0.21.23` / `v0.21.23` | `Cargo.toml:3`；`web/package.json:3`；`worker/package.json:3`；`install.sh:21`；`install.ps1:25` |
| 测试基线 | `code_cli_dispatch_test` 靠 `.env("GEMINI_API_KEY", …)` 造默认启动环境 | `tests/code_cli_dispatch_test.rs:301` |
| 本机环境 | 当前 checkout 无 `.libra/`（`libra` 命令报 `LBR-REPO-001`，用 git 等价物）；无 `.env.test`，仅 `.env.test.example` | 2026-08-24 本机核对 |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | 缺 key 提示给出不存在的 `config … add` 子命令 | 用户照抄即 `LBR-CLI-002`，二次失败 | `src/command/code.rs:1886`；`src/command/config.rs:375-393` | PS-01 |
| GAP-02 | provider 有默认值，`--provider gemini` 与「未指定」不可区分 | 无法安全引入任何按需选择；`--stdio` 的显式性判断本身就是错的 | `src/command/code.rs:519-521`、`src/command/code.rs:4446` | PS-02 |
| GAP-03 | 用户无从得知自己配了哪些凭据；只配一个也必须手打 `--provider` | 首次使用体验为纯试错 | `rg -n 'auto_selectable\|locate_env_for_target' src/` → 0 命中（2026-08-24），即无任何凭据探测实现 | PS-06、PS-03 |
| GAP-04 | 生效 provider 与 banner/preamble/usage 标签分裂（`--agent` 已现存） | 计费与可观测数据错标，UI 显示与实际驱动不一致 | `src/command/code.rs:2969`、`src/command/code.rs:2996` vs `src/command/code.rs:2732`、`src/command/code.rs:2824` | PS-04 |
| GAP-05 | 会话不记录 provider/model，`--resume` 无法继承 | 恢复线程时静默换模型，无任何提示 | `rg -n '"provider"' src/command/code.rs src/internal/ai/web/code_ui.rs src/internal/ai/web/code_ui_projection.rs` → 0 命中（2026-08-24）；`src/internal/ai/session/store.rs:1254` 仅为单测 | PS-05 |
| GAP-06 | 用户文档把凭据优先级写反 | 用户按文档排错方向相反 | `docs/commands/code.md:82`；`docs/commands/zh-CN/code.md:82` | PS-00 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-20260824.md` | 并行（DF-02 `in-progress`），共享版本面 R 写集 | 按 ER-12 串行发布；进入发布窗口前确认对方无「已 bump 未发布」态（DEP-01） |
| `plan-20260729.md` | 并行 CT-* | 写集不相交；版本面按 ER-12 串行 |
| `plan-20260715.md` | 已完成；`libra code` Web-only 主线事实源 | 消费其结论，不重开 W 卡 |
| `plan-20260822.md` | 并行 UP-01 | 写集不相交 |
| `docs/commands/code.md` | 用户文档事实源 | PS-00 订正优先级；PS-02/PS-03/PS-05 随卡同步新行为 |
| `plan-long.md` | 日期计划索引 | PS-00 增加本计划行 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 首次使用即撞不可执行提示，属用户可见硬伤 | 文案卡（PS-01）排在最前，独立发布 |
| 可行性 | 六个行为轴均可独立落地与恢复 | 拆为 PS-01..PS-06 + 一张 docs 卡（自审 R1 后） |
| 任务卡粒度 | 「取消默认 + 探测 + 配置键 + 标签收口 + resume 继承」是五个轴，不可压成一卡 | 按表面轴/生命周期轴拆分 |
| 依赖与顺序 | 全部实现卡的 I 写集都含 `src/command/code.rs` | G-10：全序串行，无并发对 |
| 完整性 | 每张代码卡自带测试 + EN/zh 文档 | G-02 |
| 安全性 | 新文案会枚举「哪些凭据已配置」 | GC-PS-01：只输出变量名与命中层，禁止值/前缀/长度 |
| 功能正确性 | 显式指定必须完全短路探测；ollama/codex 不得被自动选中 | ADR-PS-02；PS-02 AC |
| 接口兼容 | 取消默认值改变无参行为，属破坏性 | ADR-PS-01；PS-02 取 `minor` |
| 数据流与控制流 | 解析必须早于 `validate_mode_args` 与运行时分派 | ADR-PS-05 |
| 性能与容量 | 探测最多 6 次凭据查找，不得走 `resolve_env_sync` 的线程+Runtime 路径 | PS-02 Performance budget |
| 可靠性与容错 | 探测依赖开发机真实环境会导致测试不可复现 | ER-PS-02 隔离要求 |
| 可维护性 | provider 事实散在四处 match | ADR-PS-02 能力表单一事实源 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-08-25 | 用户报障 + 源码核对（2026-08-24） | 新建本计划，登记 GAP-01..GAP-06 与 PS-00..PS-05 | N/A → PS-00..PS-05 | 实施顺序、DEP、追溯表、完成判据 |
| 2026-08-25 | Codex-R1 FAIL（gpt-5.3-codex-spark） | P1×3：① 锚点用短路径/非 `file:line` 形态 → 全部规范为仓库根相对路径；② 零命中守卫是裸 `rg`，不可判定 → 改为模板五行块并新增 ER-PS-04；③ 新 test target 无登记闭环 → PS-06 补 `tests/INDEX.md` AC、两条登记守卫与「顶层 target 无需 `[[test]]`」证据行 | 无新卡 | 事实基线、GAP 表、ADR-PS-02/05、PS-00/01/02/04/06、追溯表、ER 增量、兼容与文档收口 |
| 2026-08-25 | Codex-R5 FAIL（gpt-5.5） | P1×1：ADR-PS-05 的 Context / Alternatives 与 PS-04 守卫说明引用源码时省略了 `src/command/code.rs` 前缀（只写行号），部分位置还用省略路径的续写形态 → 全部展开为完整 `path:line`；原先写成开区间的标志兼容门范围改为可核对的闭区间 `src/command/code.rs:4282-4345` | 无新卡 | ADR-PS-05、PS-04 Verification、Codex review log、修订历史 |
| 2026-08-25 | Codex-R10 PASS（gpt-5.5） | 0×P0 / 0×P1 / 0×P2；R9 三项闭环确认。本计划正文自此进入 review-PASS 状态（ER-05 / ER-PS-01 满足），可按实施顺序开工 | 无新卡 | Codex review log |
| 2026-08-25 | Codex-R9 FAIL（gpt-5.5） | P1×2：① R8 的 PS-03 事实修补没贯穿到卡片 `Description`，仍保留未限定载体的泛称 → 改写 Description；② PS-05 的「回落到 PS-02/PS-03 的解析链」仍是跨卡隐式验收（G-05）→ 展开为 ADR-PS-05 的剩余优先级链。P2×1 采纳：配置键由 `code.provider` 改名 `code.defaultProvider`，对齐 `commit.gpgSign` / `merge.verifySignatures` 的 camelCase 惯例并与 `agents.toml` 段名拉开距离 | 无新卡 | PS-03 Description/证据/AC、PS-05 AC、全文键名 16 处、Codex review log |
| 2026-08-25 | Codex-R8 FAIL（gpt-5.5） | P1×2：① **事实错误**——PS-03 称「无任何 `code.*` 配置键」，但 `agents.toml` 载体上已有 `[code.multi_agent]` / `[code.goal]` / `[code.agents.<name>]` / `[code.compaction]` / `[code.budget]`；原证据只能证明 code.rs 未调用级联读取 helper → 表述收窄为「`libra config` 的 SQLite config DB 中无 provider 默认键」，补 `agents.toml` 命名空间证据行、一条载体边界 AC（两者互不回退）与 Out of scope，AC 7/8 → 8/8；② PS-04 的「与 PS-03 完成态一致」是跨卡隐式验收（G-05）→ 展开为可自判的输出面回归断言 | 无新卡 | PS-03 Current evidence/AC/Out of scope/Granularity、PS-04 AC、粒度审计表、Codex review log |
| 2026-08-25 | Codex-R7 FAIL（gpt-5.5） | P1×2：① PS-03 / PS-06 的 `Current evidence` 曾以另一张卡的验收条目充当证据，既非可核对锚点又要求读其它卡正文（G-05）→ 删除两行，依赖关系仍由 `Dependencies` 承载；② 粒度审计表 `recovery` 列只写 `revert`，与各卡 `Granularity` 的完整恢复动作不一致（模板要求逐字段汇总）→ 七行 recovery 全部同步自对应卡 | 无新卡 | PS-03/PS-06 Current evidence、任务卡粒度审计表、Codex review log |
| 2026-08-25 | Codex-R6 FAIL（gpt-5.5） | P1×1：R5 那行修订历史本身把被修掉的省略形态原样抄进正文，导致全文归零检查自相矛盾 → 改为只描述缺陷类型、不复现该形态 | 无新卡 | 修订历史、Codex review log |
| 2026-08-25 | Codex-R4 FAIL（gpt-5.5） | P1×2：① 短锚点残留（`src/internal/ai/session/store.rs:1270` 与 `src/internal/ai/session/store.rs:879` 的续写形态）与 GAP-03 的负向事实无有界证据 → 全部展开并改为 `rg` 命令 + 观测 + 日期；② **PS-01 守卫与写集不一致**——`rg -n "config --global add" src/` 实测命中 4 行，其中 `src/internal/ai/providers/zhipu/client.rs:89`（用户可见文案）与 `src/internal/ai/providers/gemini/client.rs:102`（注释）不在 PS-01 写集内，按原卡执行会验证失败或越界改文件 → PS-01 写集扩到这两个文件，AC 增「zhipu 用户面文案」与「全仓零命中」两条，`needs_explicit_model` 一致性由 AC 降为单测，scope S → M（landing 1→3） | 无新卡 | GAP-01/GAP-03、PS-01 全字段、PS-06 追溯行、粒度审计表、Codex review log |
| 2026-08-25 | Codex-R3 FAIL（gpt-5.5） | P1×1：R1 的锚点规范在 GAP-05/GAP-06、PS-03/PS-05/PS-06 的证据列仍有残留 → 全部改为仓库根相对 `file:line`，并把「全文零命中」类负向事实换成**有界 `rg` 命令 + 观测结果 + 核对日期**。P2×1：Codex review log 的 R3 行用了 Result 列不允许的取值 → 改写并补充表头口径 | 无新卡 | 事实基线、GAP 表、PS-01/PS-03/PS-05/PS-06 证据列、Codex review log |
| 2026-08-25 | Codex-R2 PASS（gpt-5.3-codex-spark） | 0×P0 / 0×P1；闭合 2×P2：ADR-PS-05 的 `load_code_env_file` 锚点由 `src/command/code.rs:1183` 订正为 `src/command/code.rs:1184`（实测行号）；PS-02「报错码 + 文案内容」与 PS-03「显式覆盖 + 不执行探测」两条复合 AC 各拆为两条单谓词，AC 计数 7/8 → 8/8、6/8 → 7/8 | 无新卡 | ADR-PS-05、PS-02/PS-03 AC 与 Granularity、粒度审计表、Codex review log |
| 2026-08-25 | 成稿前自审 R1 | P0：PS-02 的 AC 按 G-03「独立谓词」口径实计 11 条 > 8，属「多轴伪装」。按表面轴拆开——PS-02 只保留「`--provider` 显式性与入口解析」，凭据探测与零/一/多三态拆为 PS-06 | PS-02 → PS-02, PS-06 | 实施顺序、发布窗口、Phase 2、粒度审计表、测试矩阵、追溯表、里程碑、风险表、性能摘要、完成判据 |

## 已决议设计决策

### ADR-PS-01: 取消 `--provider` 默认值，但不设为必填

- **Status:** Accepted
- **Context:** `default_value_t = Gemini` 使「显式指定」与「落到默认」不可区分（`src/command/code.rs:4446` 已因此误判），且让首次使用者撞上与自己配置无关的 `GEMINI_API_KEY`。用户已明确指示**不考虑存量兼容**。
- **Decision:** `provider: Option<CodeProvider>`，既不设默认值也不设 `required`。未指定时按 ADR-PS-05 的优先级链解析；解析不出唯一结果时报错而非猜测。
- **Alternatives considered:**
  - `required = true`：会让 `--stdio`（`src/command/code.rs:4446` 明确拒绝 `--provider`）无法调用、让 `--control stdio` 这个不构造 provider 的纯客户端被迫传值、与 `--agent` binding 重复指定，且唯一凭据场景仍要手打。
  - 保留 gemini 默认 + 仅改文案：`--stdio` 的显式性判断仍然是错的，GAP-02/GAP-03 不解决。
- **Consequences:** 无参 `libra code` 在多凭据时由「静默用 gemini」变为「报歧义错误」，属破坏性变更，由 PS-02 的 `minor` 承载。
- **Revisit when:** 出现交互式选择器需求（本计划非目标）。

### ADR-PS-02: provider 能力表是单一事实源；codex / ollama 不参与自动选择

- **Status:** Accepted
- **Context:** provider 事实目前散在 `src/command/code.rs:1741`（id 映射）、`src/command/code.rs:1766`（默认模型）、`src/command/code.rs:1808`（env 变量）、`src/command/code.rs:4282`（标志门）四处 match。而「有 key」不等于「可用」：`ollama` 仅在 base URL 指向 Cloud 时需要 key（`src/internal/ai/providers/factory.rs:297`）且缺 `--model` 会二段失败（`src/command/code.rs:1774`）；`codex` 根本没有 key 信号。
- **Decision:** 引入 `ProviderSpec { provider, id, api_key_env, key_required, needs_explicit_model, auto_selectable }` 表，作为 env 变量映射与文案枚举的唯一来源。**自动选择候选集 = `key_required && 凭据已配置`**（gemini/openai/anthropic/deepseek/kimi/zhipu 六个）；`codex` 与 `ollama` 的 `auto_selectable=false`，但必须出现在零候选文案的「免凭据路径」块。
- **Alternatives considered:** 直接对 8 个 provider 扫 env 变量——会把 `OLLAMA_API_KEY` 选成唯一候选后立刻死在 `--model is required`，也会让本地 ollama 用户被告知「你没有任何凭据」。
- **Consequences:** 新增 provider 时只需加表行；`needs_explicit_model` 与既有 `cli_default_model` 的错误分支必须保持一致（PS-01 AC）。
- **Revisit when:** 出现「key 可选但可自动选」的新 provider 形态。

### ADR-PS-03: 用户可见文案统一采用「a 模式」

- **Status:** Accepted（用户指令，2026-08-24）
- **Context:** 现有文案把结论与命令挤在一行，且命令本身不可执行（GAP-01）。
- **Decision:** 本计划新增或修改的全部用户可见诊断文案采用如下结构：

  ```
  <fatal|error>: <一句话结论，不含命令>

  Checked in order: <凭据查找链>          # 仅当结论与查找链有关时出现

  <推荐动作标题>:
      <可直接复制执行的命令>              # <可选注释>
      <可直接复制执行的命令>

  <备选动作标题>:
      <可直接复制执行的命令>
  ```

  强制规则：① 首行只给结论；② 命令块四空格缩进、一行一条、可原样粘贴执行；③ 推荐动作在前、备选在后；④ 任何情况下不输出 key 值、前缀或长度；⑤ 全部走 stderr，`--json` / `--machine` 下改为结构化字段而非自由文本。
- **Alternatives considered:** 保持单行紧凑文案——正是当前不可执行提示的成因。
- **Consequences:** PS-01/PS-02 的三类文案与 PS-05 的告警都按此结构；结构变化需修订本 ADR。
- **Revisit when:** CLI 全局采用统一的诊断渲染器。

### ADR-PS-04: 不新增 `StableErrorCode`

- **Status:** Accepted
- **Context:** 三类结局中有两类是错误：零候选（凭据缺失）与多候选（参数歧义）。
- **Decision:** 零候选复用 `LBR-AUTH-001`（`CliError::auth`，exit 128）；多候选与 `--model` 无 `--provider` 复用 `LBR-CLI-002`（`CliError::command_usage`，exit 129）。不新增变体，因此不触发 `compat_error_codes_doc_sync` 的新增行要求。
- **Alternatives considered:** 新增 `LBR-CLI-004 ambiguous-configuration`——语义与既有「参数无效或缺失」重叠，且要付一次错误码目录扩张成本。
- **Consequences:** 自动化只能靠退出码 + 文案区分两类错误；若将来需要机器可判定的歧义信号，改由 `--json` 结构化字段承载。
- **Revisit when:** 出现必须区分「歧义」与「参数非法」的自动化消费方。

### ADR-PS-05: 解析发生在 `execute()` 入口并写回 `args.provider`

- **Status:** Accepted
- **Context:** `args.provider` 在模型工厂之前就已被消费：plan-mode 默认（`src/command/code.rs:682`）、web 运行时分派（`src/command/code.rs:1207`）、banner（`src/command/code.rs:1272`）、provider match（`src/command/code.rs:2961`）、preamble（`src/command/code.rs:2824`）、usage 标签（`src/command/code.rs:2732` 与 `src/command/code.rs:2845`）、标志兼容门（`src/command/code.rs:4282-4345`）。现有 `--agent` 覆盖写在工厂内部（`src/command/code.rs:1741`），已造成标签分裂（GAP-04）。
- **Decision:** 解析在 `execute()`（按值持有 `CodeArgs`）中于 `resolve_code_preflight_working_dir` 之后、`validate_mode_args` 之前完成，并把结果写回 `args.provider`。`--stdio` 与 `--control stdio` 不进入解析（保持 `None`）。优先级：`--provider` → `--agent` binding → `--resume` 会话元数据 → 配置 `code.defaultProvider` → 自动探测。
- **Alternatives considered:** 在 `build_any_completion_model_for_args` 内部选择——永远到不了 codex 托管运行时（`src/command/code.rs:1207` 与 `src/command/code.rs:2961` 早已分派），且会把 GAP-04 的分裂面放大。
- **Consequences:** `load_code_env_file` 需从 `src/command/code.rs:1184`（`execute_web_only` 内）上提到 `src/command/code.rs:726`（`execute`）并向下传递。
- **Revisit when:** `execute()` 的模式分派被重构。

## 全局工程约束

本文继承 [`plan-template.md`](plan-template.md) v2.1 的 **GC-01..GC-12**。增量：

- **GC-PS-01 凭据零泄露:** 任何新增文案、日志、结构化输出只允许出现 provider id、环境变量名与命中层（`--env-file` / process env / repo vault / global vault），**禁止**输出 key 值、前缀、后缀或长度。
- **GC-PS-02 能力表单一事实源:** provider 的 env 变量名、是否需要 key、是否需要显式 model、是否可自动选，只允许写在 ADR-PS-02 的能力表里；禁止新增第二处 `match provider`。
- **GC-PS-03 本 checkout 的 Libra 工作流替代:** 当前 checkout 无 `.libra/`，ER-01 / ER-07 的 `libra status` / `libra add` / `libra commit` 不可用。执行时以 `git` 等价命令完成，并在卡内注明；不得因此跳过「精确暂存」（GC-12）与签名核验的意图（沿用 `plan-20260824.md` DF-01 的既有做法）。
- **GC-PS-04 stdout 协议面不可污染:** 新增提示一律 stderr。`libra code` 的 stdout 承载 banner 与自动化可解析信息，`--control stdio` 更是 JSON-RPC NDJSON 帧。

## 执行检查必备需求（强制）

本文继承 [`plan-template.md`](plan-template.md) v2.1 的 **ER-01..ER-12**。增量：

- **ER-PS-01 review 通道:** 每张实现卡在本地验收后必须取得一次独立 review `PASS`；P0/P1 必须关闭才 PASS。本计划正文的成稿 review 已按用户指示走 **Codex**（R1–R2 用 `gpt-5.3-codex-spark`，R3–R10 用 `gpt-5.5`），结论见「Codex review log」。
- **ER-PS-02 凭据探测的隔离验收:** 一切涉及「零/一/多候选」的验收命令必须在隔离环境执行——`LIBRA_CONFIG_GLOBAL_DB=<tempdir>/config.db` + 显式清空 `GEMINI_API_KEY`/`OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`DEEPSEEK_API_KEY`/`MOONSHOT_API_KEY`/`ZHIPU_API_KEY` + 不落在任何含 `.libra/libra.db` 的目录。禁止依赖开发机真实凭据，否则结果不可复现。
- **ER-PS-04 零命中守卫写法（含负向事实证据）:** 计划内一切「零命中」断言（守卫与事实基线的负向证据）都必须给出**有界的 `rg` 命令 + 观测结果 + 核对日期**，且命令中的每个路径必须存在——不存在的路径会让 `rg` 返回 `2`（而非零命中的 `1`），在简化写法下会被误判为通过。守卫本体必须使用 `plan-template.md`「Verification 判定口径」的五行块（显式取 `rc` 并区分 `rc=1` 零命中与 `rc>1` 执行失败）。简化的 `! rg …` 或 `if rg …; then exit 1; fi` 会把 `rg` 的 `>1`（非法正则、路径不存在、I/O 错误）误判为通过，**不得**作为验收证据。
- **ER-PS-03 版本面预检:** 本计划与 `plan-20260824.md` / `plan-20260729.md` 并行，进入发布窗口前必须重读 `Cargo.toml` 权威版本并确认五处一致（不一致先对齐，禁止直接 bump）。

## 实施顺序

- `PS-00 -> PS-01 -> PS-02 -> PS-06 -> PS-03 -> PS-04 -> PS-05`
- `DEP-01 -> PS-01`（发布窗口串行；不是实现阶段闸门）

**全序串行的理由（G-10）:** PS-01..PS-06 的 `Implementation write set` 全部包含 `src/command/code.rs`，两两 I–I 相交。G-10 对 I–I 无豁免通道，故不存在可并发对，只能补顺序边。PS-00 为 docs 卡，写集与实现卡不相交，但仍排在最前以免执行者按写反的文档排错。

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | incoming | 跨计划发布窗口 | `plan-20260824.md` DF-02 及后续卡的 R 写集（版本面五处） | 当值发布者 | 对方无「已 bump 未 `gh release`」状态，且五处版本面一致 | `Cargo.toml:3` + `gh release view v<semver>` | 未满足则本计划卡停在 `locally-accepted`，等待对方窗口结束；不得并发 bump（ER-12） |
| DEP-02 | incoming | 本机环境 | `.env.test` 不存在（仅 `.env.test.example`） | 执行者 | 以 `.env.test.example` 作为非敏感配置来源运行 C 组 | 2026-08-24 本机核对 | 若后续出现真实 `.env.test`，改回模板原命令并在卡内注明 |

### 发布分组与并发窗口

无家族卡。无 `REL-*` 合并发布。每张实现卡独立发布切片（`independent`）。

**并发声明:** 无可并发对（见「实施顺序」的全序串行理由）。

**发布者:** 单一当值 Agent（Eli 指派），按 ER-12 串行执行 C 组与 `gh release create`。

**发布窗口顺序:** PS-00（no-release）→ PS-01（patch）→ PS-02（**minor**）→ PS-06（patch）→ PS-03（patch）→ PS-04（patch）→ PS-05（patch）。同一时刻只允许一张卡处于「已 bump 未触发 release」状态。

## Phase 0: 文档真值

**目标:** 用户文档不再把凭据优先级写反，本计划进入日期计划索引。

**进入条件:** 无。

**退出条件:** PS-00 验收通过。

## Phase 1: 可执行的凭据文案

**目标:** 缺 key 时给出能直接跑通的命令；provider 事实收敛到能力表。

**进入条件:** Phase 0 完成。

**退出条件:** PS-01 `Acceptance=complete`。

## Phase 2: provider 解析

**目标:** 取消默认值，未指定时按零/一/多三态给出确定结果；可持久化默认。

**进入条件:** Phase 1 完成。

**退出条件:** PS-02、PS-06、PS-03 `Acceptance=complete`。

## Phase 3: 生效 provider 的一致性

**目标:** 标签与实际驱动一致；`--resume` 能继承。

**进入条件:** Phase 2 完成。

**退出条件:** PS-04、PS-05 `Acceptance=complete`。

## 任务卡

### 任务卡粒度规则（强制）

本文遵守 [`plan-template.md`](plan-template.md) v2.1 的 **G-01..G-11**。成稿后无 L/XL 卡；无 `REL-*` 合并发布；无 `EX-*` waiver。

### 字段全局默认与例外

- **Release boundary 默认:** independent
- **Task type 默认:** implementation
- **Rollback mode 默认:** revert
- **Migration and rollback 默认:** N/A：无 schema 迁移
- **Security and privacy 默认:** 继承 GC-07、GC-11、GC-PS-01、GC-PS-04
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05 同步 `docs/commands/code.md` EN + zh

**默认覆盖:**

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| PS-00 | Task type / Release boundary / Version increment / C/D coverage from | docs / no-release / N/A / PS-01 |
| PS-02 | Version increment | minor：取消 `--provider` 默认值改变无参 `libra code` 的既有成功路径，属破坏性行为变更（ADR-PS-01） |
| PS-03 | Docs and compatibility impact | 额外同步 `docs/commands/config.md` EN + zh（新增配置键） |

**规则 waiver（`EX-*`）:** 无。空表。

### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| PS-00 | docs | 凭据优先级文档真值 | revert 三份文档 | yes | yes | 4/20 | 4/20 | 0/0 | S | none | no-overlap | no-release | N/A | N/A |
| PS-01 | implementation | 缺凭据文案可执行性 | revert 单提交，四处文案/注释回到旧串 | yes | yes | 8/8 | 6/8 | 3/3 | M | PS-00, DEP-01 | 序列化于 PS-02 | independent | N/A | N/A |
| PS-02 | implementation | `--provider` 显式性与入口解析 | revert 单提交，恢复 clap 默认值 | yes | yes | 8/8 | 3/8 | 1/1 | M | PS-01 | 序列化于 PS-01 | independent | N/A | N/A |
| PS-06 | implementation | 凭据探测与零/一/多三态 | revert 单提交，回到 PS-02 的「未指定即报错」自洽态 | yes | yes | 8/8 | 6/8 | 2/2 | M | PS-02 | 序列化于 PS-02 | independent | PS-02 | N/A |
| PS-03 | implementation | 持久化默认 provider 配置键 | revert 单提交，键回到未定义（读取路径消失，已写入的键变为惰性数据） | yes | yes | 8/8 | 3/8 | 1/1 | S | PS-06 | 序列化于 PS-06 | independent | N/A | N/A |
| PS-04 | implementation | 生效 provider 的标签一致性 | revert 单提交，标签回到 args.provider | yes | yes | 6/8 | 3/8 | 1/1 | M | PS-03 | 序列化于 PS-03 | independent | N/A | N/A |
| PS-05 | implementation | 会话 provider 持久化与继承 | revert 单提交，继承路径消失、已写键变惰性数据 | yes | yes | 6/8 | 3/8 | 2/2 | M | PS-04 | 序列化于 PS-04 | independent | N/A | N/A |

### Task PS-00: 订正凭据优先级文档并索引本计划

**Task type:** docs

**Lifecycle / Acceptance:** pending / 空

**Description:** 用户文档把凭据查找顺序写成「repo-local Vault → global Vault → 进程环境」，与代码实际的「`--env-file` → 进程 env → repo vault → global vault」相反，会把排错方向引反。唯一行为轴 = 文档真值。

**Out of scope:** 任何 `src/**` 改动（需要则按 ER-03 重分类）；provider 表的新增列（由 PS-02 随卡同步）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| EN 文档写反优先级 | `docs/commands/code.md:82` |
| zh 文档写反优先级 | `docs/commands/zh-CN/code.md:82` |
| 代码实际顺序：env-file 先于进程 env | `src/command/code.rs:1575-1584` |
| 代码实际顺序：进程 env 先于 repo/global vault | `src/internal/config.rs:1152-1165` |
| 本计划未进入日期计划索引 | `docs/development/plan/plan-long.md:235` |

**Acceptance criteria:**

- [ ] `docs/commands/code.md:82` 改为 `--env-file` → 进程环境 → repo-local Vault → global Vault，并标注 `--env-file` 仅在默认 Web 启动可用
- [ ] `docs/commands/zh-CN/code.md:82` 与 EN 语义一致
- [ ] `plan-long.md` 日期计划索引增加本计划行
- [ ] 未触碰任何 `src/**` 文件

**Verification:**

- [ ] 零命中守卫（模板退出码模板；EN 旧优先级句已消失）：

  ```bash
  if rg -n "repo-local Vault, then global Vault, then the process environment" docs/commands/code.md; then
    echo "FAIL: stale EN precedence sentence still present"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [ ] 零命中守卫（模板退出码模板；zh 旧优先级句已消失）：

  ```bash
  if rg -n "先检查 repo-local Vault" docs/commands/zh-CN/code.md; then
    echo "FAIL: stale zh precedence sentence still present"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [ ] `rg -n "plan-20260825" docs/development/plan/plan-long.md`（锚点定位用，非判据）
- [ ] GC-PS-03：以 `git status --short --branch` 确认无 `src/` 越界（本 checkout 无 `.libra/`）

**Dependencies:** 无

**Deliverables:** `docs/commands/code.md`；`docs/commands/zh-CN/code.md`；`docs/development/plan/plan-long.md`

**Implementation write set:** 上述三份文档

**Release write set:** N/A

**Files likely touched:** 同上

**Rollback mode:** revert

**Estimated scope:** S

**Version increment:** N/A

**Release boundary:** no-release（产物随 PS-01 发布进入用户渠道）

**C/D coverage from:** PS-01

**Granularity:** `type=docs; axis=凭据优先级文档真值; recovery=revert 三份文档; complete=yes; self-contained=yes; AC=4/20; VER=4/20; landing=0; prod-files=0; scope=S; deps=none; writeset=no-overlap; release=no-release; split-from=N/A; exception=N/A`

### Task PS-01: 缺凭据文案改为可执行的 a 模式

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 现有缺 key 提示给出的 `libra config --global add vault.env.<KEY> <value>` 不存在对应子命令，用户照抄即二次失败。本卡把该文案按 ADR-PS-03 的 a 模式重写为可直接执行的命令，并把 provider 的 env 变量事实收敛到 ADR-PS-02 的能力表（文案的枚举来源）。唯一行为轴 = 缺凭据反馈的可执行性。

**Out of scope:** provider 解析顺序与默认值（PS-02）；配置键 `code.defaultProvider`（PS-03）；标签一致性（PS-04）；其它 provider client 的非凭据类文案（未排期，重启条件：出现同类不可执行提示）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 用户可见文案给出不存在的 `add` 子命令（两处用户面 + 两处注释，共 4 处） | `src/command/code.rs:1887`（用户面）；`src/internal/ai/providers/zhipu/client.rs:89`（用户面）；`src/command/code.rs:1671`、`src/internal/ai/providers/gemini/client.rs:102`（注释）；实测 `rg -n "config --global add" src/` → 4 行命中（2026-08-24） |
| `config` 只有 `set` 子命令与 git-compat `--add` flag | `src/command/config.rs:375-393`、`src/command/config.rs:307-309` |
| env 变量映射硬编码在一处 match | `src/command/code.rs:1806-1818` |
| ollama Cloud 缺 key 有专用文案 | `src/command/code.rs:1868-1878` |
| `codex` 无 API Key 需求；`ollama` 缺 `--model` 直接 usage 错 | `docs/commands/code.md:69-77`；`src/command/code.rs:1774-1779` |

**Acceptance criteria:**

- [ ] `src/command/code.rs:1880-1890` 的 `MissingApiKey` 文案按 a 模式渲染，推荐命令为 `libra config set --global vault.env.<ENV> <value>`
- [ ] `src/internal/ai/providers/zhipu/client.rs:86-92` 的用户可见缺凭据文案同样给出可执行命令（同一 GAP-01 缺陷的第二处用户面）
- [ ] 全仓 `config --global add` 零命中（含 `src/command/code.rs:1671`、`src/internal/ai/providers/gemini/client.rs:102` 两处注释的同款错误命令）
- [ ] 文案含「免凭据备选」块：`libra code --provider codex` 与 `libra code --provider ollama --model <name>`
- [ ] 新增 ADR-PS-02 能力表，`src/command/code.rs:1806-1818` 的 env 变量映射改由该表驱动
- [ ] Ollama Cloud 的既有专用文案保留，不被通用文案吞掉
- [ ] 错误码仍为 `LBR-AUTH-001`，未新增 `StableErrorCode`
- [ ] `docs/commands/code.md` EN + zh 的凭据配置示例与新文案一致

**Verification:**

- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::code::tests::missing_api_key_message_is_executable`（new）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib internal::ai::providers::zhipu`（覆盖 zhipu 缺凭据文案；既有 target 见 `src/internal/ai/providers/zhipu/client.rs:225`）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::code::tests::provider_spec_table_matches_default_model_branches`（new；能力表 `needs_explicit_model` 与 `cli_default_model` 的 ollama 分支一致）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --test code_cli_dispatch_test`
- [ ] 手工（ER-PS-02 隔离环境）：把文案中的 `libra config set --global vault.env.GEMINI_API_KEY <value>` 原样执行，退出码 0
- [ ] 零命中守卫（模板退出码模板；不可执行的 `config … add` 提示已消失）：

  ```bash
  if rg -n "config --global add" src/; then
    echo "FAIL: unexecutable `config --global add` hint still present"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```


**Dependencies:** PS-00；DEP-01（发布窗口）

**Deliverables:** N/A

**Implementation write set:** `src/command/code.rs`；`src/internal/ai/providers/zhipu/client.rs`；`src/internal/ai/providers/gemini/client.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** Inherited

**Rollback mode:** revert

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=缺凭据文案可执行性; recovery=revert 单提交，四处文案/注释回到旧串; complete=yes; self-contained=yes; AC=8/8; VER=6/8; landing=3; prod-files=3; scope=M; deps=PS-00,DEP-01; writeset=序列化于 PS-02; release=independent; split-from=N/A; exception=N/A`

### Task PS-02: 取消 `--provider` 默认值并在入口解析生效 provider

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** `--provider` 目前有 clap 默认值，导致「显式指定」与「未指定」不可区分（`src/command/code.rs:4446` 已因此误判），无参启动也总是撞 `GEMINI_API_KEY`。本卡改为 `Option<CodeProvider>`，并按 ADR-PS-05 在 `execute()` 入口完成解析与写回；未指定且无其它来源时报可执行的 usage 错误。唯一行为轴 = `--provider` 的显式性与生效 provider 的入口解析。**本卡拆出 `PS-06`**（凭据探测与零/一/多三态），本卡不含任何凭据探测。

**Out of scope:** 凭据探测与零/一/多三态（由 PS-06 承接）；持久化默认键 `code.defaultProvider`（PS-03，届时插入优先级第 4 位）；会话元数据继承（PS-05，优先级第 3 位）；标签一致性（PS-04）；交互式选择器（永久非目标，DEFER-PS-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| clap 默认值 | `src/command/code.rs:519-521` |
| 显式性靠 `!= Gemini` 反推 | `src/command/code.rs:4446` |
| `--control stdio` 提前 return，不构造 provider | `src/command/code.rs:729-767`；`src/command/code.rs:4258` |
| `--stdio` 经 `reject_non_tui_flags` 拒绝 `--provider` | `src/command/code.rs:4272`；`src/command/code.rs:4446` |
| 解析必须早于的消费点 | `src/command/code.rs:682`、`src/command/code.rs:1207`、`src/command/code.rs:1272`、`src/command/code.rs:2961`、`src/command/code.rs:2824`、`src/command/code.rs:4282` |
| `--agent` 绑定当前在工厂内部覆盖 provider | `src/command/code.rs:1791-1800` |

**Acceptance criteria:**

- [ ] `provider` 改为 `Option<CodeProvider>`，解析结果在 `validate_mode_args` 之前写回 `args.provider`
- [ ] `src/command/code.rs:4446` 改判 `is_some()`，`libra code --stdio --provider gemini` 由静默接受改为被拒
- [ ] 未指定且无 `--agent` 绑定 → `LBR-CLI-002`
- [ ] 该错误的文案按 a 模式列出全部可选 provider 与对应配置命令
- [ ] 优先级按 ADR-PS-05 第 1/2 位（`--provider` → `--agent` binding）；两者冲突时 `LBR-CLI-002`
- [ ] `--stdio` 与 `--control stdio` 不进入解析，零凭据下仍按各自既有语义运行
- [ ] banner、plan-mode 默认与 web 运行时分派均取写回后的生效值
- [ ] `docs/commands/code.md` EN + zh 记录新行为

**Verification:**

- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::code::tests::provider_resolution_entry`（new）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --test code_cli_dispatch_test`
- [ ] 手工（ER-PS-02 隔离环境）：零凭据下 `libra code --control stdio` 与 `libra code --stdio` 均按各自既有语义运行，不因 provider 缺失而失败

**Dependencies:** PS-01（消费其能力表与 a 模式渲染）

**Deliverables:** N/A

**Implementation write set:** `src/command/code.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** Inherited；`COMPATIBILITY.md` 仅当 `libra code` 的行级描述失真时更新（开工时核对）

**Rollback mode:** revert

**Migration and rollback:** N/A：无 schema 迁移。行为回滚 = revert 单提交后 `--provider` 恢复默认值语义。

**Security and privacy（PS-01）:** 文案不得含 key 值、前缀或长度（GC-PS-01），由 `missing_api_key_message_is_executable` 的 redaction 断言覆盖。

**Security and privacy:** 新增文案只列 provider id 与配置命令模板，不读取也不打印任何 key（GC-PS-01）。

**Performance budget:** 本卡不引入任何凭据查找；解析只读已解析的 CLI 参数与 `--agent` profile。

**Estimated scope:** M

**Version increment:** minor（ADR-PS-01：破坏无参 `libra code` 的既有成功路径）

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=--provider 显式性与入口解析; recovery=revert 单提交，恢复 clap 默认值; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=PS-01; writeset=序列化于 PS-01; release=independent; split-from=N/A; exception=N/A`

### Task PS-03: 持久化默认 provider 配置键 `code.defaultProvider`

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** PS-02 之后，配置了多个凭据的用户每次都要手打 `--provider`，而 `libra config` 的 SQLite config DB 里当前没有任何 provider 默认键（`[code.*]` 前缀在另一载体 `agents.toml` 上另有含义，见下表证据）。本卡新增 `code.defaultProvider`，插入解析优先级第 4 位（`--provider` → `--agent` binding → 会话元数据 → **`code.defaultProvider`** → 自动探测）。唯一行为轴 = 持久化默认 provider 的读取与校验。

**Out of scope:** 其它 `code.*` 配置键（未排期，重启条件：出现第二个需要持久化的 code 选项）；`libra config` 的通用键校验框架（永久非目标，本卡只校验自己的键）；`agents.toml` 的 `[code.*]` profile 段语义（永久非目标：本卡不读、不写、不迁移该文件）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `libra config` 键空间（SQLite config DB）中无任何 provider 默认键 | `rg -n 'read_cascaded_config_value' src/command/code.rs` → 0 命中（2026-08-24） |
| 键名取 `code.defaultProvider`（对齐 `commit.gpgSign` / `merge.verifySignatures` 的 camelCase 惯例，且不与 `agents.toml` 的段名形近） | `src/command/history_config.rs:64`、`src/command/history_config.rs:73` |
| `[code.*]` 前缀在**另一载体** `agents.toml` 中已被 profile 配置占用（`[code.multi_agent]` / `[code.goal]` / `[code.agents.<name>]` / `[code.compaction]` / `[code.budget]`），与 config DB 是两套独立键空间 | `src/internal/ai/agent/profile/config.rs:10-14`；载体说明见 `src/internal/ai/agent/profile/config.rs:4`、`src/internal/ai/agent/profile/config.rs:647` |
| 级联读取 helper 已存在 | `src/internal/config.rs:1242-1250` |
| 保留命名空间只有 `upgrade.*` | `src/command/config.rs:887-893` |

**Acceptance criteria:**

- [ ] `libra config set --global code.defaultProvider deepseek` 后，无参 `libra code` 使用 deepseek 且不打印唯一候选提示
- [ ] repo-local `code.defaultProvider` 覆盖 global（`read_cascaded_config_value` 级联）
- [ ] 非法取值 → `LBR-CLI-002`，a 模式文案列出全部合法取值
- [ ] 显式 `--provider` 覆盖 `code.defaultProvider` 配置值
- [ ] 配置命中时不执行凭据探测
- [ ] 配置命中的 provider 缺凭据 → `LBR-AUTH-001`，文案只列该 provider
- [ ] `code.defaultProvider` 只落在 `libra config` 的 SQLite config DB，不读写 `agents.toml` 的 `[code.*]` 段，两者互不回退
- [ ] `docs/commands/code.md` 与 `docs/commands/config.md` 的 EN + zh 记录该键、取值域、优先级位次，并写明与 `agents.toml` `[code.*]` 的载体区别

**Verification:**

- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --test code_provider_resolution_test config_default_key`（new）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::code::tests::provider_config_key`（new）
- [ ] 手工（ER-PS-02 隔离环境）：`LIBRA_CONFIG_GLOBAL_DB=<tempdir>/config.db` 下写入合法值生效、写入非法值退出码 129

**Dependencies:** PS-06（消费其优先级链与三态文案；I–I 序列化）

**Deliverables:** N/A

**Implementation write set:** `src/command/code.rs`；`tests/code_provider_resolution_test.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`；`docs/commands/config.md`；`docs/commands/zh-CN/config.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** 额外同步 `docs/commands/config.md` EN + zh

**Rollback mode:** revert

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=持久化默认 provider 配置键; recovery=revert 单提交，键回到未定义（读取路径消失，已写入的键变为惰性数据）; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=PS-06; writeset=序列化于 PS-06; release=independent; split-from=N/A; exception=N/A`

### Task PS-06: 凭据探测与零/一/多三态解析（自 PS-02 拆出）

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** PS-02 之后，未指定 `--provider` 一律报错，用户仍不知道自己配了哪些凭据。本卡在 `src/internal/config.rs` 新增 `locate_env_for_target`（返回值 + 命中层，`resolve_env_for_target` 委托它、既有优先级链不变，由 Verification 的 `locate_env` 单测判定），并按候选数给出零/一/多三种确定结局：零 → 可执行的配置指引；一 → 自动选用并说明理由；多 → 歧义错误并列出候选。唯一行为轴 = 未指定 provider 时的凭据探测结果。自 `PS-02` 拆出（G-09）。

**Out of scope:** provider 参数面与写回（PS-02 已交付）；持久化默认键（PS-03）；provider 健康探活/key 有效性校验（永久非目标，DEFER-PS-02）；交互式选单（DEFER-PS-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 凭据链已实现但不返回命中层 | `src/command/code.rs:1575-1584`；`src/internal/config.rs:1152-1165` |
| 同步解析每次 spawn 线程 + 新建 Runtime + 开 SQLite | `src/internal/config.rs:1100-1122` |
| `ollama` 仅 Cloud 需要 key，且无默认 model | `src/internal/ai/providers/factory.rs:285-300`；`src/command/code.rs:1774-1779` |
| `codex` 无 API Key 需求 | `docs/commands/code.md:69-77` |
| `tests/INDEX.md` 尚无 `code_provider_resolution_test` 行 | `rg -n 'code_provider_resolution_test' tests/INDEX.md` → 0 命中（2026-08-24） |
| 顶层 `tests/*.rs` 由 Cargo 默认发现，无需 `Cargo.toml` `[[test]]`（仅 `tests/compat/*` 需注册） | `rg -n 'code_cli_dispatch_test\|agent_review_workflow_test' Cargo.toml` → 0 命中（2026-08-24）；`CLAUDE.md:138-148`「Testing Guidelines」 |

**Acceptance criteria:**

- [ ] 未指定 + 零候选 → `LBR-AUTH-001`，a 模式文案含 `Checked in order` 链与免凭据备选块
- [ ] 未指定 + 唯一候选 → 以该 provider 启动，stderr 输出 provider id、变量名与命中层，stdout 无新增行
- [ ] 未指定 + 多候选 → `LBR-CLI-002`，按 provider id 字典序列出全部候选及其命中层
- [ ] 上述全部输出均不含 key 值、前缀或长度（GC-PS-01）
- [ ] 未指定 + `--model` 且需落到探测 → `LBR-CLI-002`，理由为 provider/model 配对不可推断
- [ ] `codex` 与 `ollama` 的 `auto_selectable=false`，任何候选数下都不会被自动选中
- [ ] `docs/commands/code.md` EN + zh 记录三态行为与候选判定口径
- [ ] `tests/INDEX.md` 登记 `code_provider_resolution_test` 行（GC-06；该 target 位于 `tests/` 顶层，由 Cargo 默认发现，**不需要** `Cargo.toml` `[[test]]` 注册——只有 `tests/compat/*` 才需要）

**Verification:**

- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --test code_provider_resolution_test`（new target）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib internal::config::tests::locate_env`（new）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::code::tests::provider_detection`（new；含「显式指定时探测次数为 0」断言）
- [ ] 必须命中守卫（命中为通过判据）：`rg -n "code_provider_resolution_test" tests/INDEX.md` 至少 1 行命中
- [ ] 零命中守卫（模板退出码模板；顶层 target 不得误注册 `[[test]]`）：

  ```bash
  if rg -n "code_provider_resolution_test" Cargo.toml; then
    echo "FAIL: top-level test target must not be registered as [[test]]"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [ ] 手工（ER-PS-02 隔离环境）：零/一/多三态各跑一次，记录 sanitized 输出与退出码（128 / 0 / 129）

**Dependencies:** PS-02（消费其 `Option<CodeProvider>` 参数面、优先级链与写回点）

**Deliverables:** N/A

**Implementation write set:** `src/command/code.rs`；`src/internal/config.rs`；`tests/code_provider_resolution_test.rs`；`tests/INDEX.md`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** Inherited

**Rollback mode:** revert

**Security and privacy（PS-01）:** 文案不得含 key 值、前缀或长度（GC-PS-01），由 `missing_api_key_message_is_executable` 的 redaction 断言覆盖。

**Security and privacy:** 探测只读环境变量名与配置键是否存在；渲染层只接受 provider id / 变量名 / 命中层三类字段，禁止把值传入文案层（GC-PS-01）。

**Performance budget:** 未指定 provider 时最多 6 次凭据查找（env-file / 进程 env 命中即短路）；必须走 `execute()` 已有的 async 上下文，**禁止** `resolve_env_sync` 的「每次 spawn 线程 + 新建 tokio Runtime + 开 SQLite」路径；显式指定时探测次数为 0（单测断言）。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=凭据探测与零/一/多三态; recovery=revert 单提交，回到 PS-02 的「未指定即报错」自洽态; complete=yes; self-contained=yes; AC=8/8; VER=6/8; landing=2; prod-files=2; scope=M; deps=PS-02; writeset=序列化于 PS-02; release=independent; split-from=PS-02; exception=N/A`

### Task PS-04: 生效 provider 的标签一致性收口

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** `build_any_completion_model_for_args` 返回的生效 provider id 在两个调用点被丢弃为 `_`，banner、UI 快照、system preamble 与 usage 计费记录仍按 `args.provider` 打标。`--agent` 绑定覆盖 provider 后，这些标签与实际驱动会话的 provider 不一致——这是先于本计划就存在的缺陷，PS-02 引入解析后必须收口，否则错标面被放大。唯一行为轴 = 可观测标签跟随生效 provider。

**Out of scope:** usage 计费的历史数据回填（永久非目标：历史记录不可追溯重写）；`--agent` profile 的绑定语义（不改）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 返回的 provider id 被丢弃 | `src/command/code.rs:2969`、`src/command/code.rs:2996` |
| UI 快照标签取 `args.provider` | `src/command/code.rs:2732-2737` |
| usage 记录标签取同一变量 | `src/command/code.rs:2845` |
| system preamble 取 `args.provider` | `src/command/code.rs:2824` |
| `--agent` 覆盖发生在工厂内部 | `src/command/code.rs:1791-1800` |

**Acceptance criteria:**

- [ ] `src/command/code.rs:2969` 与 `src/command/code.rs:2996` 消费工厂返回的 provider id，不再丢弃为 `_`
- [ ] `CodeUiProviderInfo.provider`、`UsageContext.provider`、`system_preamble` 三处均按生效 provider 取值
- [ ] `--agent` 携带绑定时，banner、UI 快照、usage 记录三者一致且等于绑定的 provider
- [ ] 未使用 `--agent` 时，banner、UI 快照与 usage 记录三者仍取入口解析写回的 `args.provider`，输出面相对本卡改动前无差异（由 `code_cli_dispatch_test` 回归覆盖）
- [ ] 无新增公开 flag 或错误码；banner 行格式不变，只有取值变化
- [ ] `docs/commands/code.md` EN + zh 的 `--agent` 段说明标签跟随生效 provider

**Verification:**

- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --test code_provider_resolution_test agent_binding_labels`（new）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::code::tests::provider_labels`（new）
- [ ] 零命中守卫（模板退出码模板；provider id 不再被丢弃。模式已在 2026-08-24 基线上实测命中 2 行——`src/command/code.rs:2969` 与 `src/command/code.rs:2996` 的解构是跨行书写，任何含 `= build_any_completion_model_for_args` 的单行模式都会零命中而**假通过**）：

  ```bash
  if rg -n "let \(model, model_name, _\)" src/command/code.rs; then
    echo "FAIL: factory provider id still discarded"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```


**Dependencies:** PS-03（I–I 序列化；本卡消费 PS-02 写回的生效 provider）

**Deliverables:** N/A

**Implementation write set:** `src/command/code.rs`；`tests/code_provider_resolution_test.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** Inherited

**Rollback mode:** revert

**Security and privacy（PS-01）:** 文案不得含 key 值、前缀或长度（GC-PS-01），由 `missing_api_key_message_is_executable` 的 redaction 断言覆盖。

**Security and privacy:** usage 记录只写 provider id 与 model id，不含凭据（GC-PS-01）。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=生效 provider 的标签一致性; recovery=revert 单提交，标签回到 args.provider; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=PS-03; writeset=序列化于 PS-03; release=independent; split-from=N/A; exception=N/A`

### Task PS-05: 会话持久化 provider/model 与 `--resume` 继承

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** live 路径从不把 provider 写入会话元数据，`--resume` 因此无法知道该线程当初用的是谁；用户记错时会静默把历史消息喂给另一个模型。本卡在建会话时持久化 provider/model，并把它接入解析优先级第 3 位，显式冲突时告警。唯一行为轴 = 会话的 provider 归属持久化与继承。

**Out of scope:** 浏览器内 session 热切换（永久非目标，见 `plan-20260824.md` DEFER-DF-01）；历史会话的元数据回填（尚未排期，重启条件：出现依赖历史归属的统计需求）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| live 路径无 `"provider"` 写入 | `rg -n '"provider"' src/command/code.rs src/internal/ai/web/code_ui.rs src/internal/ai/web/code_ui_projection.rs` → 0 命中（2026-08-24）；`src/internal/ai/session/store.rs:1254` 与 `src/internal/ai/session/store.rs:1270` 均在 `mod tests`（`mod tests` 起于 `src/internal/ai/session/store.rs:879`） |
| store 仅在单测里手塞该键 | `src/internal/ai/session/store.rs:1254`、`src/internal/ai/session/store.rs:1270` |
| metadata 为自由 map，可承载新键 | `src/internal/ai/session/store.rs:738-765` |
| 会话创建/恢复入口 | `src/command/code.rs:2380-2410` |

**Acceptance criteria:**

- [ ] 新建会话时把生效 provider 与 model 写入 session metadata
- [ ] `--resume` 且未显式指定 provider 时从元数据继承（解析优先级第 3 位，先于 `code.defaultProvider`）
- [ ] 显式 `--provider` 与记录不符时输出 a 模式告警到 stderr 并继续执行，不 fatal
- [ ] 元数据缺失的旧会话继续按 ADR-PS-05 的剩余优先级链解析（`--provider` → `--agent` binding → `code.defaultProvider` → 自动探测），不因缺失而 fatal
- [ ] 写入的元数据不含任何凭据信息（GC-PS-01）
- [ ] `docs/commands/code.md` EN + zh 记录继承规则与告警行为

**Verification:**

- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --test code_provider_resolution_test resume_inherits_provider`（new）
- [ ] `LIBRA_SKIP_WEB_BUILD=1 cargo test --lib internal::ai::session::store::tests::`（回归既有 metadata 用例）
- [ ] 手工（ER-PS-02 隔离环境）：建会话 → 清空该 provider 凭据并配置另一个 → `--resume` 继承原 provider 并报其缺凭据（而非静默换模型）

**Dependencies:** PS-04（I–I 序列化；本卡消费其生效 provider 取值）

**Deliverables:** N/A

**Implementation write set:** `src/command/code.rs`；`src/internal/ai/session/store.rs`；`tests/code_provider_resolution_test.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** Inherited

**Rollback mode:** revert

**Migration and rollback:** N/A：metadata 为自由 map，新增键对旧会话是缺失即回落，无 schema 迁移；revert 后新写入的键变为惰性数据，不影响读取路径。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=会话 provider 持久化与继承; recovery=revert 单提交，继承路径消失、已写键变惰性数据; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=2; prod-files=2; scope=M; deps=PS-04; writeset=序列化于 PS-04; release=independent; split-from=N/A; exception=N/A`

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | 文案渲染、能力表一致性、解析优先级、标签取值 | `cargo test --lib command::code::tests::<filter>` |
| 集成 | 零/一/多三态、显式短路、`--model` 守卫、配置键、resume 继承 | `code_provider_resolution_test`（new）、`code_cli_dispatch_test` |
| 兼容 | help/EXAMPLES 横幅、错误码目录、版本面 | `compat_help_examples_banner`、`compat_error_codes_doc_sync`、`compat_version_surface_sync` |
| 安全 | 文案零凭据泄露、stdout 不被污染 | `code_provider_resolution_test` 的 redaction 断言 + `--control stdio` 手工证据 |
| 性能 | 显式指定时探测次数为 0；未指定时不走线程+Runtime 路径 | PS-06 单测断言（调用计数） |
| live/gated | 无本计划强制 L2/L3 | N/A |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| PS-00 | GAP-06；`docs/commands/code.md:82` vs `src/internal/config.rs:1152-1165` | 三份文档 | code EN/zh 优先级订正 + plan-long 索引 | rg 零命中守卫 ×2 |
| PS-01 | GAP-01；用户报障 | `src/command/code.rs` 文案 + 能力表 | code EN/zh 配置示例 | `missing_api_key_message_is_executable` |
| PS-02 | GAP-02；`src/command/code.rs:519-521`、`src/command/code.rs:4446` | `src/command/code.rs` 入口解析与写回 | code EN/zh 新行为 | `provider_resolution_entry` |
| PS-06 | GAP-03（`rg -n 'auto_selectable\|locate_env_for_target' src/` → 0 命中）；自 PS-02 拆出 | `src/command/code.rs` 探测 + `src/internal/config.rs` 命中层 | code EN/zh 三态 + `tests/INDEX.md` | `code_provider_resolution_test` |
| PS-03 | GAP-03 持久化面 | `src/command/code.rs` 配置读取 | code + config EN/zh | `config_default_key` |
| PS-04 | GAP-04；`src/command/code.rs:2969`、`src/command/code.rs:2996` | `src/command/code.rs` 标签取值 | code EN/zh `--agent` 段 | `agent_binding_labels` |
| PS-05 | GAP-05；session store 无 live 写入 | `src/command/code.rs` + `src/internal/ai/session/store.rs` | code EN/zh resume 段 | `resume_inherits_provider` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | PS-00 | no-release 文档（随 PS-01 进入渠道） | revert 三份文档 |
| M1 | PS-01 | patch + `gh release create` | revert；文案回到旧串（仍不可执行，但无新增风险） |
| M2 | PS-02、PS-06、PS-03 | minor + patch ×2，各自 `gh release create` | revert；`--provider` 恢复默认值语义、探测路径消失 |
| M3 | PS-04、PS-05 | 各 patch + `gh release create` | revert；标签回到 `args.provider`，继承路径消失 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| PS-06 已发布但探测误选（如把 ollama 选为唯一候选） | 用户显式 `--provider` 可绕过 | 立即 revert 该卡；能力表 `auto_selectable` 补齐后前滚 | 让用户在 `--model is required` 上二段失败 |
| PS-02 解析写回遗漏某个消费点 | banner 与实际不一致 | 按 ER-10 判定：落在本卡轴内则就地修并重跑 G-*，否则立 `FIX-*` | 保留分裂状态并宣告完成 |
| PS-03 配置键读取失败（DB 损坏） | 回落到自动探测 | 视为「未配置」并继续，不 fatal | 因配置读取失败阻断启动 |
| PS-05 元数据写入失败 | 会话仍可运行，仅无法继承 | 记录 warn 并继续 | 因元数据写入失败中断会话 |
| 任一卡 bump 与并发计划撞车 | 版本面短暂不一致 | 按 ER-PS-03 先对齐再 bump | 并发 bump 或手改 `Cargo.lock` |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 探测把「有 key」当「可用」，选中 ollama/codex | 高：启动即二段失败 | ADR-PS-02 的 `auto_selectable=false` + AC 断言 | PS-06 |
| 解析位置过晚，codex 托管运行时永远选不到 | 高：`--provider codex` 语义被破坏 | ADR-PS-05 固定在 `validate_mode_args` 之前 | PS-02 |
| 新文案泄露凭据片段 | 高：secret 进日志/终端记录 | GC-PS-01 + 专门的 redaction 断言 | PS-01、PS-06 |
| 测试依赖开发机真实凭据 | 中：CI 与本机结果不一致 | ER-PS-02 隔离要求（临时 global DB + 显式清空六个变量） | PS-02..PS-06 |
| 提示写到 stdout 破坏 `--control stdio` 帧 | 高：自动化客户端解析失败 | GC-PS-04 + 手工证据 | PS-02、PS-06 |
| 与并发计划同时 bump | 中：版本面不一致 | ER-12 串行 + ER-PS-03 预检 + DEP-01 | 全部 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| 显式 `--provider` 启动 | 0 次探测 | 与今日一致 | 不得因本计划新增任何查找 | PS-06 单测调用计数 |
| 未指定 provider 启动 | ≤ 6 次凭据查找（env-file/进程 env 命中即短路） | 一次性，仅启动路径 | 不得走 `resolve_env_sync` 的线程+Runtime+SQLite 路径 | PS-06 单测 + 代码走查 |
| `code.defaultProvider` 读取 | 1 次级联配置读 | 一次性 | 复用 `read_cascaded_config_value`，不新开连接池 | PS-03 单测 |
| 会话元数据写入 | 1 个 map 键 | 每会话一次 | 不新增文件或表 | PS-05 单测 |

## 兼容与文档收口

- [ ] `docs/commands/code.md` + `docs/commands/zh-CN/code.md`：PS-00 优先级订正；PS-02 新行为；PS-03 配置键；PS-04 `--agent` 标签；PS-05 resume 继承
- [ ] `docs/commands/config.md` + zh：PS-03 的 `code.defaultProvider` 键
- [ ] `docs/error-codes.md`：无新增码（ADR-PS-04），仅当 `LBR-AUTH-001` / `LBR-CLI-002` 的示例列需要补一行时更新
- [ ] `COMPATIBILITY.md`：`libra code` 为 Libra-only 扩展命令，除非其行级描述失真否则 N/A（开工时核对）
- [ ] `tests/INDEX.md`：PS-06 新增 `code_provider_resolution_test` target
- [ ] `Cargo.toml` `[[test]]`：`code_provider_resolution_test` 位于 `tests/` 顶层，由 Cargo 默认发现，**无需**注册（证据：`Cargo.toml` 现无 `code_cli_dispatch_test` 等顶层 target 条目；仅 `tests/compat/*` 需要）
- [ ] `plan-long.md`：PS-00 增加本计划索引行

## Codex review log

Result 只允许 `PASS` 或 `FAIL`；未产出结论的尝试（限流、崩溃）不占 Result，只写进 Evidence。模型：R1–R2 用 `gpt-5.3-codex-spark`，R3 起按用户指示改用 `gpt-5.5`（`codex exec --skip-git-repo-check -m <model>`）。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| Codex-R1 | 本文件对照 `plan-template.md` v2.1 + 当前源码（7 维度） | FAIL | 0×P0 / 3×P1：① 锚点用短路径与非 `file:line` 形态；② 零命中守卫是裸 `rg`，不可判定；③ 新 test target 无登记闭环 | 无 P2 | 本会话 `codex exec`；sanitized 结论已抄入「修订历史」 |
| Codex-R2 | R1 修订后复审 + 闭环核验 | PASS | 0×P0 / 0×P1；P1-1/P1-2/P1-3 全部 CLOSED | 2×P2 已闭合：`src/command/code.rs:1183` → `src/command/code.rs:1184` 行漂移；PS-02/PS-03 各一条含 `；` 的复合 AC 拆为单谓词 | 本会话 `codex exec`；P2 修订见「修订历史」2026-08-25 Codex-R2 行 |
| Codex-R3 | R2 的 P2 闭合后确认（模型改为 `gpt-5.5`） | FAIL | 0×P0 / 1×P1：GAP-05/GAP-06 与 PS-01 AC、PS-03、PS-05、PS-06 的证据列仍是短路径形态（缺仓库根前缀与行锚），且负向事实缺有界 `rg` 证据 | 1×P2 已闭合：本表 Result 列只允许 PASS/FAIL，原 R3 行的「未取得（BLOCKED）」已改写 | 首次以 `gpt-5.3-codex-spark` 发起于 2026-08-24 18:34 CST 时命中该模型用量上限（按 ER-10 不 blind retry），遂按用户指示改用 `gpt-5.5` 完成本轮 |
| Codex-R4 | R3 修订后复审（`gpt-5.5`，逐条实跑计划内 `rg` 证据） | FAIL | 0×P0 / 2×P1：短锚点与 GAP-03 负向证据残留；PS-01 守卫扫 `src/` 但两处命中不在其写集内 | 1×P2 已闭合：R3 行的受影响范围列补全 PS-01/PS-03/PS-05 | 本会话 `codex exec -m gpt-5.5`；处置见「修订历史」2026-08-25 Codex-R4 行 |
| Codex-R5 | R4 修订后复审（`gpt-5.5`） | FAIL | 0×P0 / 1×P1：ADR-PS-05 与 PS-04 守卫说明引用源码时省略了文件路径前缀 | 无 P2 | 本会话 `codex exec -m gpt-5.5`；处置见「修订历史」2026-08-25 Codex-R5 行 |
| Codex-R6 | R5 修订后复审（`gpt-5.5`） | FAIL | 0×P0 / 1×P1：R5 的修订历史行复现了被修掉的省略形态，与「全文归零」自相矛盾 | 无 P2 | 本会话 `codex exec -m gpt-5.5`；处置见「修订历史」2026-08-25 Codex-R6 行 |
| Codex-R7 | R6 修订后复审（`gpt-5.5`） | FAIL | 0×P0 / 2×P1：跨卡证据引用违反 G-05；粒度审计表 recovery 列与各卡 Granularity 漂移 | 无 P2 | 本会话 `codex exec -m gpt-5.5`；处置见「修订历史」2026-08-25 Codex-R7 行 |
| Codex-R8 | R7 修订后复审（`gpt-5.5`） | FAIL | 0×P0 / 2×P1：PS-03 的 `code.*` 命名空间事实错误；PS-04 存在跨卡隐式 AC | 无 P2 | 本会话 `codex exec -m gpt-5.5`；处置见「修订历史」2026-08-25 Codex-R8 行 |
| Codex-R9 | R8 修订后复审（`gpt-5.5`） | FAIL | 0×P0 / 2×P1：PS-03 Description 仍复现未限定的 `code.*` 泛称；PS-05 存在跨卡隐式 AC | 1×P2 已采纳：键名改为 `code.defaultProvider` | 本会话 `codex exec -m gpt-5.5`；处置见「修订历史」2026-08-25 Codex-R9 行 |
| Codex-R10 | R9 修订后复审（`gpt-5.5`，含逐条实跑 `rg` 证据 + 七维复扫） | **PASS** | 0×P0 / 0×P1；R9 的 3 项全部 CLOSED | 0×P2 | 本会话 `codex exec -m gpt-5.5`；结论：AC/VER 实计与各卡 Granularity 分子一致、粒度审计表 15 列一致、无 rc=2 证据命令、无新 P0/P1 |

## Claude review log

Result 只允许 `PASS` 或 `FAIL`。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| — | 待执行（ER-PS-01） | — | — | — | — |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-PS-01 | 交互式 provider 选择器（多候选时的终端选单） | 需要 TTY 检测与非交互回落两条路径，是独立行为轴 | 出现明确的人机交互需求且 `--json`/自动化回落已定义 | 后续日期计划 |
| DEFER-PS-02 | provider 健康探活（key 有效性/网络握手） | 会把启动路径变成网络依赖，与「快速失败」冲突 | 出现「配置了但 key 已失效」的高频报障 | 后续日期计划 |
| DEFER-PS-03 | 历史会话 provider 元数据回填 | 历史归属不可追溯重建 | 出现依赖历史 provider 归属的统计需求 | 后续日期计划 |
| DEFER-PS-04 | 其它 `code.*` 持久化配置键（model / context / approval-policy） | 每个键都是独立的默认值语义轴 | 出现第二个需要持久化的 code 选项 | 后续日期计划 |

## 完成判据

- [ ] 粒度审计表 7 张卡均达标；无 L/XL、无未登记合并发布、无 I–I 未消解冲突
- [ ] 非延后任务（PS-00..PS-06）均为 `Lifecycle=done` 且 `Acceptance=complete`
- [ ] 各卡 Verification 已运行并记录；`(new)` 用例真实执行
- [ ] 计划完成门：`cargo +nightly fmt --all --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`cargo test --all`（本机无 `.env.test`，按 DEP-02 以 `.env.test.example` 作为非敏感配置来源）
- [ ] 文档 / compat / error-codes / `tests/INDEX.md` 按「兼容与文档收口」完成
- [ ] review 最终 `PASS`，P0/P1 关闭（ER-PS-01）
- [ ] 会发版卡版本面五处一致：PS-01/PS-06/PS-03/PS-04/PS-05 各 `patch + 1`、PS-02 为 `minor`；`gh release create` 均已触发
- [ ] 修订历史记录成稿后的全部规范性变更
- [ ] `plan-long.md` 已索引本计划
