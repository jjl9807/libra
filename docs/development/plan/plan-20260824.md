# plan-20260715 延期项收口计划（2026-08-24）

> 模板版本: v2.1（按 `plan-template.md` 成稿；本计划为新建计划，整份符合 v2.1）
> 前置计划: [`plan-20260715.md`](plan-20260715.md)（RT-01 / Web-only 主线 **已完成**；本计划只承接其 DEFER 与 closeout 残差）
> Long-track ID: `plan-long.md` **RT-01 后续**

## 文档职责

本文把 `plan-20260715.md` 完成判据勾选之后**仍然未实现**的能力收成可执行任务卡。2026-08-24 源码核对结论：W0–W6 与 WIO-01..03 共 71 张卡已落地，`libra code` 默认 Web Code UI，`AgentRuntimeWorker` 是唯一状态机 owner，Code TUI / `code-control` / `--web` 别名已随 v0.20.0 breaking minor 删除。未实现部分是计划明文延期的 DEFER 项，以及一条已文档化的 fail-closed 残差（skill activate）。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。**禁止重开已关闭的 W0–W6 / WIO 卡。**

### 适用范围

- `review --fix` / `investigate fix` 的 AgentRuntime serialized mutating bridge（0715 DEFER-01；0714 PD-01 的可执行拆分）。
- SSE wire 默认切到 v2，以及满足 DEFER-08 清单后的 v1 snapshot stream 物理移除。
- `test_create_tree` object-index repair-marker 共享锁隔离（0715 DEFER-10）。
- Code UI `POST /api/code/skills/activate` 从 discoverability-only 升级为 provider 可消费的激活效果。
- 事实源刷新：`tracing/code.md` C10 过期缺口、`plan-20260714.md` PD-01 过期 BLOCKED 句、`plan-20260715.md` DEFER-09 关闭、`plan-long.md` 日期计划索引。

### 非目标

- 不重做 `plan-20260715` 已关闭的 W 卡、不恢复 Code TUI、不重写 `AgentRuntimeWorker`。
- 不在 Code 路径复制第二套 mutation queue / approval / sandbox（ADR-DF-02）。
- 不实现独立 `libra mcp --stdio`（DEFER-02）、MCP 授权门闭环（DEFER-03）、非 loopback 远程写面（DEFER-04）、单短横 `-control`（DEFER-05，永久不做）。
- 不等待、不伪造 Codex app-server 的完整 user-input 纳管（DEFER-07）。
- 不实现浏览器内 in-process AgentRuntime session 热切换（`SESSION_RESUME_REQUIRES_RESTART` 保留；CLI `--resume` 已工作）。
- 不实现 0714 PD-02 scoped review、PD-03 cloud tombstone 等非 0715 DEFER 范围。

### 成功定义

- `libra review --fix` 与 `libra investigate fix` 在 runtime 可用时进入 serialized mutating workflow；无 runtime / 无权限时仍 `LBR-AGENT-010`；untrusted seed 先触发 `LBR-AGENT-011`。
- 省略 `wire` 的 `GET /api/code/events` 默认为 v2；显式 v1 在烘焙窗口内仍可用；窗口满足后 v1 分支删除。
- 满载 `cargo test --all` 不再因 `command::commit::test::test_create_tree` 的共享锁超时失败。
- discoverable skill 的 `POST /api/code/skills/activate` 产生 provider 可消费的激活效果，不再固定 `SKILL_ACTIVATION_UNSUPPORTED`。
- 所有非延后任务 `Lifecycle=done` 且 `Acceptance=complete`；计划完成门全绿。

## 事实基线

> 锚点核对日期：**2026-08-24**。crate `0.21.22`。开工时按 ER-02 刷新行号。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 代码入口 | `libra code` 默认 `execute_web_only`；`--control stdio` 为 client-only discovery shim；`--stdio` 为 MCP-only legacy | `src/command/code.rs:726-781` |
| Runtime owner | `AgentRuntimeWorker` 串行 turn queue；confirmed-plan 经 `submit_confirmed_plan_execution` | `src/internal/ai/runtime/worker.rs`；`plan_execution.rs:69` |
| TUI | `src/internal/tui/` 不存在；`Cargo.toml` 无 ratatui/crossterm | 目录缺失；`compat_agent_architecture_guard` |
| 用户命令 | `review --fix` / `investigate fix` 无条件 `LBR-AGENT-010` | `src/command/agent/review.rs:259-265,430-432`；`investigate.rs:293,1103` |
| SSE 默认 | 省略 `wire` 时 `DEFAULT_CODE_UI_SSE_WIRE_VERSION = V1`；内置 SPA 已 `?wire=2` | `src/internal/ai/web/sse_wire.rs:43,85`；`web/src/lib/code-ui/sse-resilience/v2-wire.ts:23` |
| Skill activate | `ExecutionControlService::skill_activate` 只做 A0-07 可发现性校验后 `Ok(())`；HTTP 仍返回 `SKILL_ACTIVATION_UNSUPPORTED` | `src/internal/ai/runtime/execution_control.rs:221-239`；`docs/commands/code.md:437` |
| Session resume | CLI `--resume` 走 JSONL fold；`POST /api/code/session/resume` 在证明可加载后 422 `SESSION_RESUME_REQUIRES_RESTART` | `src/internal/ai/web/mod.rs:1720-1748` |
| 错误码 | `LBR-AGENT-010` / `011` 已分配；`PLAN_EXECUTION_NOT_AVAILABLE` 仅目录保留 | `src/utils/error.rs:267-269,389`；`docs/error-codes.md` |
| 测试 | `review_fix_returns_unsupported_until_bridge_ready` 钉死 fail-closed；`test_create_tree` 曾在满载套件因 object-index repair-marker 锁超时 | `tests/agent_review_workflow_test.rs:543`；`src/command/commit.rs:3608` |
| 文档漂移 | `tracing/code.md` C10 仍写「W2-04 缺口」；0714 PD-01 仍写 W1-01/W6-02 未完成 | `docs/development/tracing/code.md:119`；`plan-20260714.md:838-844` |
| 版本面 | 五处均为 `0.21.22` / `v0.21.22`（并发节点已 bump；开工按 ER-02 重读 `Cargo.toml`） | `Cargo.toml:3`；`install.ps1:25` |
| 外部参照 | 0715 W6-02 已登记 serialized fix 入口就绪 | `docs/development/tracing/agent.md:886-890` |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | 事实源把已关闭的 W2-04 / 已满足的 PD-01 前置写成现状 | 执行者按过期 BLOCKED 拒绝开工，或把 confirmed-plan 当未接线 | `tracing/code.md:119`；`plan-20260714.md:838` | DF-01 |
| GAP-02 | `review --fix` / `investigate fix` 无条件 010 | 0715 成功定义中的 mutating fix 入口对 agent CLI 不可用 | `review.rs:430-432` | DF-03、DF-09、DF-04 |
| GAP-03 | SSE 默认仍是 v1 全量 snapshot | 未指定版本的 automation 走热路径全量包；DEFER-08 删除前置不满足 | `sse_wire.rs:43` | DF-05、DF-06、DF-08 |
| GAP-04 | `test_create_tree` 共享锁 | 满载 `cargo test --all` 可超时，污染任意卡的 C 组 | `commit.rs:3608`；0715 DEFER-10 | DF-02 |
| GAP-05 | skill activate 无 provider 效果 | Web 面板可搜索但不能激活 | `execution_control.rs:221-239` | DF-07 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-20260715.md` | 前置；主线已完成 | 只承接 DEFER-01/08/10 与 skill 残差；不重开 W 卡；DF-01 关闭其 DEFER-09 |
| `plan-20260714.md` PD-01 | 原 owner，卡为 L 且 BLOCKED 文案过期 | 本计划 DF-03/DF-09/DF-04 是其**可执行拆分**（outgoing DEP-01）；0714 PD-01 在 DF-04 完成后改为文档收口，禁止平行实现 |
| `plan-20260729.md` | 并行；共享版本面 R 写集 | ER-12 串行发布；冲突时让位对方当值发布窗口 |
| `plan-20260818.md` | 已完成 bridge | 不触碰 `agent_bridge` |
| `plan-20260822.md` | 并行 UP-01 | 写集不相交；版本面仍按 ER-12 串行 |
| `docs/development/tracing/agent.md` | A0-05 重启条件事实源 | 消费，不复制；DF-03 接线后同步「bridge 已落地」 |
| `plan-long.md` RT-01 | 已完成；后续 DEFER 独立立项 | DF-01 把本计划链进日期计划索引 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 0715 主线已完成；剩下的是用户可见 fail-closed 与明确延期项 | 不重开 W 卡 |
| 可行性 | PD-01 原卡 L，必须拆成 review admission、review outcomes、investigate 接线 | DF-03/DF-09/DF-04 |
| 任务卡粒度 | SSE 按协议轴拆默认切换与物理删除；删除依赖公开烘焙 | DF-05/DF-06/DF-08 + DEP-02 |
| 依赖与顺序 | 文档解阻先于实现；fix bridge 与 SSE 写集不相交可并行 | 见实施顺序 |
| 完整性 | 每张代码卡自带测试与 EN/zh | G-02 |
| 安全性 | mutating fix 必须走 hardening + approval/sandbox；untrusted seed 先 011 | ADR-DF-02 |
| 功能正确性 | 无 runtime 时保持 010，禁止假成功 | DF-03 AC |
| 接口兼容 | SSE 默认切换是公开行为，须保留显式 v1 一版 | ADR-DF-03 |
| 数据流与控制流 | 不新建 session store；事件仍 JSONL | GC-02 |
| 性能与容量 | v2 默认后省略 wire 的客户端不再吃全量 snapshot | DF-06 |
| 可靠性与容错 | 共享锁隔离不得改 commit 生产语义 | DF-02 |
| 可维护性 | 0714 PD-01 不得与本计划双实现 | DEP-01 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-08-24 | 0715 closeout 源码核对 | 新建本计划，承接 DEFER-01/08/10 与 skill 残差 | N/A → DF-01..DF-08 | 实施顺序、DEP、追溯表 |
| 2026-08-24 | Claude-R1 FAIL | P0：DF-03/DF-04 把四条独立 outcome 断言压进一条 AC，真实计数超 G-03 上限。按推荐拆分维度把 review 的 admission/fail-closed 与 controlled-execution outcomes 切开；investigate 不再复述四条 outcome。P1：实施顺序补 DEP-03/DEP-01 边。P2：DEP-02 Owner、版本面 0.21.22 | DF-03 → DF-03, DF-09 | 实施顺序、粒度审计表、Phase 1、追溯表、完成判据、Claude review log |
| 2026-08-24 | Claude-R2 FAIL | P0：DF-07 与 DF-08 I–I 相交（`web/mod.rs` + code.md EN/zh）却无依赖边，且 DEP-02 可由并行计划满足使 DF-08 抢跑。补 `DF-07 -> DF-08`。P2：事实基线 crate 行仍写 0.21.21 | 无新卡 | 实施顺序、DF-08 Dependencies/Granularity、并发声明、发布窗口、Claude review log |
| 2026-08-24 | Claude-R3 FAIL | P0：R1 补边把 outgoing DEP-01 写成 `DEP-01 -> DF-04`，与「DF-04 complete 后才产生 DEP-01」自指。改为 `DF-04 -> DEP-01`，DF-04 Dependencies 只留 DF-09 | 无新卡 | 实施顺序、DF-04 Dependencies/AC/Granularity、Claude review log |
| 2026-08-24 | Claude-R4 PASS | 0×P0 / 0×P1；R1–R3 闭包确认 | 无新卡 | Claude review log |

## 已决议设计决策

### ADR-DF-01: 不重开 plan-20260715 已关闭任务卡

- **Status:** Accepted
- **Context:** 0715 Checkpoint A–D 与完成判据已勾选；源码已是 Web-default + runtime owner。
- **Decision:** 本计划只新增 `DF-*` 卡。发现 0715 卡面与代码不一致时，修文档或另立 `FIX-*`，不得把已关闭 W 卡改回 pending。
- **Alternatives considered:** 在 0715 上追加卡——与「计划完成」矛盾，且模板增量迁移会把 71 张旧卡拖进 v2.1。
- **Consequences:** 0715 保持历史完成态；本文件是唯一开工事实源。
- **Revisit when:** 发现 0715 主线功能实际回归。

### ADR-DF-02: mutating fix 只走既有 AgentRuntime 入口

- **Status:** Accepted
- **Context:** 0715 ADR-CODE-04 / F37：A0-05 bridge 依赖 runtime，禁止第二套 queue。
- **Decision:** DF-03/DF-04 经 `AgentRuntimeHandle::submit`（或与 `submit_confirmed_plan_execution` 同级的 typed mutating 入口）+ `runtime::hardening` + 既有 approval/sandbox/ACL。无 runtime 时仍 `LBR-AGENT-010`。
- **Alternatives considered:** review CLI 直接调 tool loop——绕过串行化与 lease。
- **Consequences:** 共享 `src/internal/ai/runtime/` 写集，`DF-03 -> DF-09 -> DF-04` 串行。
- **Revisit when:** runtime 入口重构。

### ADR-DF-03: SSE v1 删除必须满足 DEFER-08 五条清单

- **Status:** Accepted
- **Context:** 0715 ADR-CODE-08：v2 成为默认后再保留 ≥1 个成功公开 patch。
- **Decision:** DF-05 迁 automation/compat；DF-06 切服务端默认；DEP-02 登记烘焙；DF-08 才删 v1。禁止 DF-06 与 DF-08 合并发布。
- **Alternatives considered:** 直接删 v1——破坏未升级的 automation。
- **Consequences:** DF-08 在烘焙完成前保持 `blocked`。
- **Revisit when:** 清单第 2 条（automation 已迁）被证伪。

### ADR-DF-04: in-process session 热切换本计划不做

- **Status:** Accepted
- **Context:** `POST /api/code/session/resume` 已能证明 thread 可加载，但热替换 live worker/lease/durability 是独立行为轴。
- **Decision:** 保留 `SESSION_RESUME_REQUIRES_RESTART`；用户路径仍是 `libra code --resume`。登记 DEFER-DF-01。
- **Alternatives considered:** 本计划做热切换——会与 lease/W1-05 indeterminate 缠在一起，超出 focused session。
- **Consequences:** Web 线程列表切换需重启进程。
- **Revisit when:** 独立设计通过 Claude/Codex review。

## 全局工程约束

本文继承 `plan-template.md` v2.1 的 **GC-01..GC-12**（现状核实、单一事实源、Git/Libra 边界、错误契约、文档同步、测试索引、安全默认 fail-closed、原子性、并发与资源、性能预算、禁止生产 panic、精确暂存）。增量：

- **GC-DF-01** 禁止重开 0715 已关闭 W/WIO 卡。
- **GC-DF-02** 禁止为 fix bridge 新建平行 mutation queue、approval store 或 sandbox。
- **GC-DF-03** 写面继续 loopback + token + lease；本计划不打开非 loopback 写 API。

## 执行检查必备需求（强制）

本文继承 `plan-template.md` v2.1 的 **ER-01..ER-12**（开工安全检查、先核对后实现、粒度门、A/B/C/D 验收、review PASS、文档兼容、Libra-native 提交签名、每卡 `patch + 1` + 版本面五处 + 工具链 `Cargo.lock` + `gh release create`、push 失败策略、有界重试、证据卫生、串行发布）。增量：

- **ER-DF-01** 本计划实现卡与本计划正文的只读 review 以 **Claude Code** 为强制通道（用户指令）；P0/P1 必须关闭才 PASS。Codex 可作为交叉复审，不替代 Claude PASS。
- **ER-DF-02** DF-08 开工前必须在任务记录附上 DEP-02 的公开 release 证据（tag + `gh release view`），否则保持 `blocked`。

## 实施顺序

- `DEP-03 -> DF-01`
- `DEP-03 -> DF-03`
- `DF-01 -> DF-03 -> DF-09 -> DF-04`
- `DF-04 -> DEP-01`（outgoing 移交：DF-04 完成后 0714 PD-01 才可文档收口；DEP-01 不是 DF-04 的开工闸门）
- `DF-01 -> DF-02`
- `DF-01 -> DF-05 -> DF-06 -> DF-07`
- `DF-06 -> DEP-02 -> DF-08`
- `DF-07 -> DF-08`（I–I：`src/internal/ai/web/mod.rs` 与 `docs/commands/code.md` EN+zh；G-10 无豁免）

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | outgoing | 跨计划 | `plan-20260714.md` PD-01 | 0714 当值 Agent | DF-04 `complete` 后 0714 PD-01 只做文档收口，不得另写 bridge | 本计划 DF-04 发布记录 | 0714 若抢先实现：停本计划 DF-03/DF-09/DF-04，按 ER-10 立 FIX 对账，禁止双 queue |
| DEP-02 | incoming | 上游 release | DF-06 之后的**下一个**成功公开 patch release（非 DF-06 自身） | 下一个成功公开 patch 的发布者（本计划或并行计划，以 `gh release view` 证据为准；不要求必须是本计划发布者） | `gh release view v<semver>` 成功且 artifact 可获取；该版本默认 wire=v2、显式 v1 仍可用 | tag + release.yml D 组 | 未满足则 DF-08 保持 `blocked`，不得用本地 commit 计数替代 |
| DEP-03 | incoming | 跨计划 | `plan-20260715.md` W6-02 重启条件登记 | 已交付 | `tracing/agent.md` 写明 serialized mutating 入口锚点 | `agent.md:886-890`（2026-08-24） | 若锚点消失：DF-03 置 `blocked` 并先修文档 |

### 发布分组与并发窗口

无家族卡。无 `REL-*` 合并发布。

**并发声明:** DF-02 与 DF-03 实现写集不相交，可并发；DF-03/DF-09/DF-04 共享 `src/internal/ai/runtime/` 与 review/investigate CLI，按 `DF-03 -> DF-09 -> DF-04` 串行；DF-05 → DF-06 → DF-07 → DF-08 共享 `sse_wire.rs` / `web/mod.rs` / `docs/commands/code.md`，含 `DF-07 -> DF-08`。DF-01 为 docs，先于全部实现卡。

**发布者:** 单一当值 Agent（Eli 指派），按 ER-12 串行 C 组与 `gh release create`。

**发布窗口顺序:** DF-01（no-release）→ DF-02 → DF-03 → DF-09 → DF-04 → DF-05 → DF-06 → DF-07 →（DEP-02）→ DF-08。DF-08 必须在 DF-07 之后（I–I）。若 DF-02 与 DF-03 实现并行，发布仍按此序，同一时刻只一张卡处于「已 bump 未 gh release」。

## Phase 0: 事实源解阻

**目标:** 过期 BLOCKED / C10 缺口不再误导开工。

**进入条件:** 0715 closeout 已提交；DEP-03 锚点仍在。

**退出条件:** DF-01 验收通过。

## Phase 1: 测试基础设施与 mutating fix

**目标:** 满载测试不再被共享锁误杀；`review --fix` / `investigate fix` 进入 runtime。

**进入条件:** Phase 0 完成。

**退出条件:** DF-02、DF-03、DF-09、DF-04 `Acceptance=complete`（含各自 D 组）。

## Phase 2: SSE 默认 v2 与 skill 激活

**目标:** 省略 wire 走 v2；skill activate 有 provider 效果。

**进入条件:** Phase 0 完成（可与 Phase 1 实现并行，发布串行）。

**退出条件:** DF-05、DF-06、DF-07 complete。

## Phase 3: SSE v1 物理移除

**目标:** 删除 v1 snapshot stream。

**进入条件:** DEP-02 满足。

**退出条件:** DF-08 complete。

## 任务卡

### 任务卡粒度规则（强制）

本文遵守 `plan-template.md` v2.1 的 **G-01..G-11**。成稿后无 L/XL 卡；无 `REL-*` 合并发布；无 `EX-*` waiver。

### 字段全局默认与例外

- **Release boundary 默认:** independent
- **Task type 默认:** implementation
- **Rollback mode 默认:** revert
- **Migration and rollback 默认:** N/A：无 schema 迁移
- **Security and privacy 默认:** 继承 GC-07、GC-11、GC-DF-02、GC-DF-03
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05 同步 EN + zh

**默认覆盖:**

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| DF-01 | Task type / Release boundary / Version increment / C/D coverage from | docs / no-release / N/A / DF-02 |
| DF-08 | Rollback mode / Version increment | immutable-release：已发布 v1 客户端只能靠新版本；minor：删除公开 wire 版本 |

**规则 waiver（`EX-*`）:** 无。空表。

### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| DF-01 | docs | 过期事实源刷新 | revert 文档 | yes | yes | 4/20 | 3/20 | 0/4 | S | DEP-03 | no-overlap | no-release | N/A | N/A |
| DF-02 | implementation | test_create_tree 锁隔离 | revert | yes | yes | 4/8 | 3/8 | 2/3 | S | DF-01 | no-overlap | independent | N/A | N/A |
| DF-03 | implementation | review --fix admission/fail-closed | revert | yes | yes | 5/8 | 4/8 | 3/6 | M | DF-01,DEP-03 | 序列化于 DF-09 | independent | N/A | N/A |
| DF-09 | implementation | review --fix controlled execution outcomes | revert | yes | yes | 5/8 | 4/8 | 2/5 | M | DF-03 | 序列化于 DF-03 | independent | DF-03 | N/A |
| DF-04 | implementation | investigate fix CLI 接线 | revert | yes | yes | 6/8 | 3/8 | 2/5 | M | DF-09 | 序列化于 DF-09 | independent | N/A | N/A |
| DF-05 | implementation | automation/compat 默认 SSE v2 | revert | yes | yes | 4/8 | 3/8 | 2/6 | M | DF-01 | 序列化于 DF-06 | independent | N/A | N/A |
| DF-06 | implementation | 服务端 SSE 默认 v2 | revert | yes | yes | 4/8 | 3/8 | 2/4 | S | DF-05 | 序列化于 DF-05 | independent | N/A | N/A |
| DF-07 | implementation | skill activate provider 效果 | revert | yes | yes | 4/8 | 3/8 | 3/6 | M | DF-06 | 序列化于 DF-06 | independent | N/A | N/A |
| DF-08 | removal | 物理删除 SSE v1 | 发新版本恢复 v1（immutable-release） | yes | yes | 4/8 | 3/8 | 2/5 | M | DF-06,DF-07,DEP-02 | 序列化于 DF-07 | independent | N/A | N/A |

### Task DF-01: 刷新 0715/0714/tracing 过期事实源

**Task type:** docs

**Lifecycle / Acceptance:** done / complete

**Description:** 把已关闭的 0715 主线与已满足的 PD-01 前置写成当前态，避免执行者按过期缺口开工。唯一行为轴 = 文档真值。

**Out of scope:** 任何 `src/**` 改动（需要则按 ER-03 重分类）；不重写 0715 任务卡勾选。

**Baseline evidence（执行前，已由本卡消除）:**

| 事实 | 证据 |
|---|---|
| C10 仍写 W2-04 缺口 | `docs/development/tracing/code.md:119` |
| 0714 PD-01 仍写 W1-01/W6-02 未完成 | `plan-20260714.md:838-844` |
| 0715 DEFER-09 仍像未开工 | `plan-20260715.md:2802` |
| W6-02 入口已登记 | `docs/development/tracing/agent.md:886-890` |

**Acceptance criteria:**

- [x] `tracing/code.md` C10 标明 W2-04 已接线，剩余仅本计划 DEFER / DF 卡
- [x] `plan-20260714.md` PD-01 改为「重启条件已满足，可执行拆分见 plan-20260824 DF-03/DF-09/DF-04」
- [x] `plan-20260715.md` DEFER-09 标 CLOSED（WIO-01..03）
- [x] `plan-long.md` 日期计划索引增加本计划行

**Verification:**

- [x] 过期原句守卫（模板零命中口径）：`rg -n "W2-04 的 Web confirmed-plan execution handoff" docs/development/tracing/code.md` 为零命中
- [x] `rg -n "plan-20260824" docs/development/plan/plan-long.md docs/development/plan/plan-20260714.md`（锚点定位用，非判据）
- [x] 当前 checkout 无 `.libra`（`libra status` 返回 `LBR-REPO-001`），以等价的 `git status --short --branch` 确认无 `src/` 越界

**Dependencies:** DEP-03

**Deliverables:** `docs/development/tracing/code.md`；`docs/development/plan/plan-20260714.md`；`docs/development/plan/plan-20260715.md`；`docs/development/plan/plan-long.md`

**Implementation write set:** 上述四文件

**Release write set:** N/A

**Files likely touched:** 同上

**Rollback mode:** revert

**Estimated scope:** S

**Version increment:** N/A

**Release boundary:** no-release（产物随 DF-02 发布进入用户渠道）

**C/D coverage from:** DF-02

**Granularity:** `type=docs; axis=过期事实源刷新; recovery=revert 四份文档; complete=yes; self-contained=yes; AC=4/20; VER=3/20; landing=0; prod-files=0; scope=S; deps=DEP-03; writeset=no-overlap; release=no-release; split-from=N/A; exception=N/A`

### Task DF-02: 隔离 `test_create_tree` object-index repair-marker 锁

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-08-24；Claude Code DF-02 R2 PASS）

**Description:** 让 `command::commit::test::test_create_tree` 在满载 `cargo test --all` 下不再因进程外 object-index repair-marker 共享锁超时。唯一行为轴 = 测试隔离；不改变 commit 生产语义。

**Out of scope:** 生产 object-index 算法（永久非目标）；其它 commit 测试的性能（尚未排期）。

**Baseline evidence（执行前）:**

| 事实 | 证据 |
|---|---|
| 用例存在且 `#[serial]` | `src/command/commit.rs:3608` |
| 0715 将满载超时登记为 DEFER-10 | `plan-20260715.md:2803` |

**Implementation evidence:**

| 事实 | 证据 |
|---|---|
| fixture storage 不再位于 `.libra/objects`，因而不会加入 repository object-index repair work | `src/command/commit.rs` 的 `test_create_tree` 使用 tempdir 内 `tree-fixture-objects` |
| 回归断言在调用 `create_tree` 后验证 repair-lock 目录不存在 | `src/command/commit.rs` 的 `tree fixture storage must not enqueue object-index repair work` |
| 满载 C 组曾暴露一个独立的测试进程 CWD 竞态；该测试现在将 Git probe 与 `git config` 子进程固定在其 tempdir | `tests/command/config_test.rs` 的 `test_config_upgrade_mode_import_skips_reserved` |
| 满载 C 组的 workspace-lease failpoint 用例现在先证明跨连接已观察到 doctor-1 的 autocommit，才启动 doctor-2 写入 | `tests/workspace_lease_test.rs` 的 `takeover_reports_its_own_fence_even_if_another_lands_immediately_after` |

**Acceptance criteria:**

- [x] 该测试使用进程私有 / tempdir 范围的 lock 或 index 路径，不与并行套件争用全局 `/tmp` marker
- [x] commit 生产路径行为不变（无新公开 flag/错误码）
- [x] 0715 DEFER-10 关闭句写入 0715 延后表
- [x] `tests/INDEX.md` 已复核：未新增或重命名 target，既有描述未失真，无需修改

**Verification:**

- [x] 环境中无 `.env.test`，以仓内 `.env.test.example` 作为可用非敏感配置运行：`LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::commit::test::test_create_tree -- --exact --test-threads=1`（1 passed）
- [x] 同一配置下：`LIBRA_SKIP_WEB_BUILD=1 cargo test --lib command::commit::test -- --test-threads=1`（34 passed）
- [x] 本卡 C 组：`LIBRA_SKIP_WEB_BUILD=1 cargo test --all`（修复 workspace-lease 可见性前置后，完整默认套件运行至文档测试收尾并正常退出）
- [x] 发布前质量门：`LIBRA_SKIP_WEB_BUILD=1 cargo +nightly fmt --all --check`、`LIBRA_SKIP_WEB_BUILD=1 cargo clippy --all-targets --all-features -- -D warnings`（均退出 0）
- [x] 版本面：六处同步为 `0.21.23` / `v0.21.23`；`LIBRA_SKIP_WEB_BUILD=1 cargo test --test compat_version_surface_sync`（2 passed）

**Dependencies:** DF-01

**Deliverables:** N/A

**Implementation write set:** `src/command/commit.rs` 的对应 `#[cfg(test)]`；`tests/command/config_test.rs`（仅修复满载 C 组中该测试子进程的 tempdir CWD 隔离）；`src/utils/client_storage.rs`（仅当锁路径确在此）；`docs/development/plan/plan-20260715.md` DEFER-10 关闭句

**Release write set:** Inherited

**Files likely touched:** `src/command/commit.rs`、`tests/command/config_test.rs`、`src/utils/client_storage.rs`、`docs/development/plan/plan-20260715.md`

**Rollback mode:** revert

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=test_create_tree 锁隔离; recovery=revert 测试/锁路径改动，生产 commit 语义不变; complete=yes; self-contained=yes; AC=4/8; VER=3/8; landing=2; prod-files=3; scope=S; deps=DF-01; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

### Task DF-03: `review --fix` admission 与 fail-closed 闸门

**Task type:** implementation

**Lifecycle / Acceptance:** release-pending / complete

**Description:** 把 `libra review --fix` 从无条件 `LBR-AGENT-010` 改为经既有 AgentRuntime helper **admit**：有 runtime 则进入 serialized 入口，无 runtime / 无权限仍 010，untrusted seed 在任何 mutation 前 011。不交付成功 patch 或四类执行结局（DF-09）。唯一行为轴 = review CLI admission / fail-closed。

**Out of scope:** approval/sandbox/tool-failure/成功 patch 四类执行结局（由 DF-09 承接）；`investigate fix`（DF-04 承接）；scoped `--checkpoint`（0714 PD-02，尚未排期）；Codex/外部 agent 直接改树（永久非目标）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `--fix` 在 active Code runtime 存在时进入 admission helper，并以 deferred JSON 返回 | `src/command/agent/review.rs:486-496` |
| admission helper 复用现有 loopback、token 与 controller-lease hardening；固定请求不含外部 findings | `src/internal/ai/runtime/fix_bridge.rs:28-140`；`src/command/code_control.rs` |
| 无 runtime 时的 fail-closed 错误构造 | `src/command/agent/review.rs:262-284` |
| integration guard 覆盖 admission、无 runtime 与互斥 scope | `tests/agent_review_workflow_test.rs:621-804` |

**Acceptance criteria:**

- [x] `--fix` 在 AgentRuntime 可构建时 submit 到 runtime helper，不再无条件 010
- [x] 无 runtime 时仍 `LBR-AGENT-010`，JSON 错误结构稳定
- [x] untrusted seed 在任何 mutation 前 `LBR-AGENT-011`
- [x] 不新建 mutation queue；helper 复用既有 loopback/token/controller-lease hardening，且 admission 不运行 mutating tool
- [x] `docs/commands/review.md` 与 zh-CN、`docs/error-codes.md`、`docs/development/tracing/agent.md` 写明 admission 已接线、执行结局归 DF-09

**Verification:**

- [x] `source .env.test && cargo test --test agent_review_workflow_test review_fix_bridge_admission` (new; direct test runner: 1 passed)
- [x] `source .env.test && cargo test --test agent_review_workflow_test review_fix_returns_unsupported_until_bridge_ready`（与 admission / scope guard 合并执行：3 passed）
- [x] `source .env.test && cargo test --test command_test review_investigate_fix_json_errors` (1 passed)
- [x] `source .env.test && cargo test --test ai_runtime_contract_test` (46 passed)

**Review evidence:** Claude Code DF-03 R1、R2 的 P2 反馈已修正；R3 对当前 diff 给出 `VERDICT: PASS`，无 P0/P1 或必须修正项。R3 的 controller detach/result race 维持 fail-closed，untrusted-seed guard 暂为 helper-level（当前 CLI 无外部 seed 参数）；写集遗漏的 `code_control.rs` 已在下方修正。release-pending 期间不得启动 DF-09。

**Dependencies:** DF-01, DEP-03

**Deliverables:** N/A

**Implementation write set:** `src/command/agent/review.rs`；`src/command/code_control.rs`（仅复用既有 loopback/token/controller hardening）；`src/internal/ai/runtime/`（新增 `fix_bridge.rs` 或等价 helper，禁止第三套 queue）；`tests/agent_review_workflow_test.rs`；`docs/commands/review.md`；`docs/commands/zh-CN/review.md`；`docs/error-codes.md`；`docs/development/tracing/agent.md`

**Release write set:** Inherited

**Files likely touched:** 同上；`tests/INDEX.md` 仅描述失真时

**Rollback mode:** revert

**Security and privacy:** observed review findings 不得直接写工作树；provenance env 剥离后再 submit；fail-closed 无 runtime

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=review --fix admission/fail-closed; recovery=revert CLI+helper，行为回到无条件 010; complete=yes; self-contained=yes; AC=5/8; VER=4/8; landing=3; prod-files=6; scope=M; deps=DF-01,DEP-03; writeset=序列化于 DF-09; release=independent; split-from=N/A; exception=N/A`

### Task DF-04: `investigate fix` 接入同一 mutating helper

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 把 `libra investigate fix` 接到 DF-03/DF-09 交付的同一 runtime helper，不复制 queue。唯一行为轴 = investigate CLI 接线。四类执行结局由 helper 契约（DF-09）覆盖，本卡不重列。

**Out of scope:** review `--fix` admission（DF-03）与四类执行结局（DF-09）；PD-02 checkpoint scope（尚未排期）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| investigate fix 无条件 010 | `src/command/agent/investigate.rs:1103` |
| 与 review 共用语义注释 | `investigate.rs:291-293` |

**Acceptance criteria:**

- [ ] `investigate fix` 调用与 review 相同的 helper，不复制 queue
- [ ] 不出现第二份 hardening/approval 表
- [ ] 无 runtime 时仍 `LBR-AGENT-010`
- [ ] untrusted seed 在任何 mutation 前 `LBR-AGENT-011`
- [ ] EN/zh `docs/commands/investigate.md` 同步
- [ ] 0714 PD-01 改为仅文档收口（本卡完成后产生 outgoing DEP-01，不是本卡开工前置）

**Verification:**

- [ ] `source .env.test && cargo test --test agent_investigate_workflow_test investigate_fix_bridge` (new)
- [ ] `source .env.test && cargo test --test command_test review_investigate_fix_json_errors`
- [ ] `source .env.test && cargo test --test agent_review_workflow_test review_fix_bridge_approval_denied`

**Dependencies:** DF-09

**Deliverables:** N/A

**Implementation write set:** `src/command/agent/investigate.rs`；`src/internal/ai/runtime/`（只接线，不改 DF-03/DF-09 语义）；`tests/agent_investigate_workflow_test.rs`；`docs/commands/investigate.md`；`docs/commands/zh-CN/investigate.md`；`docs/development/plan/plan-20260714.md`（PD-01 收口句）

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** revert

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=investigate fix CLI 接线; recovery=revert investigate 接线，review helper 保持; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=2; prod-files=5; scope=M; deps=DF-09; writeset=序列化于 DF-09; release=independent; split-from=N/A; exception=N/A`

### Task DF-09: `review --fix` 受控执行结局（自 DF-03 拆出）

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 在 DF-03 helper 已能 admit 的前提下，交付 `review --fix` 四条可独立失败的受控执行结局。唯一行为轴 = controlled execution outcomes。自 DF-03 拆出（G-09）。

**Out of scope:** admission/010/011（DF-03 已交付）；`investigate fix` CLI（DF-04 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `--fix` 今日在 admit 前即返回，无执行结局 | `src/command/agent/review.rs:430-432` |
| helper 由 DF-03 引入 | `src/internal/ai/runtime/`（开工刷新） |

**Acceptance criteria:**

- [ ] approval denied：零工作树 mutation，稳定错误
- [ ] sandbox denied：零工作树 mutation，稳定错误
- [ ] tool failure：不静默成功，进入既有可判定失败/repair 契约
- [ ] 成功 patch：经 hardening/approval/sandbox/ACL 写入
- [ ] EN/zh `docs/commands/review.md` 描述上述四条结局

**Verification:**

- [ ] `source .env.test && cargo test --test agent_review_workflow_test review_fix_bridge_approval_denied` (new)
- [ ] `source .env.test && cargo test --test agent_review_workflow_test review_fix_bridge_sandbox_denied` (new)
- [ ] `source .env.test && cargo test --test agent_review_workflow_test review_fix_bridge_tool_failure` (new)
- [ ] `source .env.test && cargo test --test agent_review_workflow_test review_fix_bridge_success_patch` (new)

**Dependencies:** DF-03

**Deliverables:** N/A

**Implementation write set:** `src/internal/ai/runtime/`（helper 执行结局，不改 DF-03 admission 语义）；`src/command/agent/review.rs`（仅当执行路径仍在 CLI）；`tests/agent_review_workflow_test.rs`；`docs/commands/review.md`；`docs/commands/zh-CN/review.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** revert

**Security and privacy:** 四条结局均不得绕过 hardening；denied 路径零 mutation

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=review --fix controlled execution outcomes; recovery=revert 执行结局，admission 保持 fail-closed; complete=yes; self-contained=yes; AC=5/8; VER=4/8; landing=2; prod-files=5; scope=M; deps=DF-03; writeset=序列化于 DF-03; release=independent; split-from=DF-03; exception=N/A`

### Task DF-05: 内置 automation 与 compat 默认消费 SSE v2

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 让 `--control stdio` 与 Code UI remote/compat 矩阵在未显式点名 v1 时走 v2 delta/cursor。唯一行为轴 = 消费端默认 v2。不改服务端 `DEFAULT_CODE_UI_SSE_WIRE_VERSION`。

**Out of scope:** 服务端默认切换（DF-06 承接）；物理删除 v1（DF-08 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| SPA 已 `wire=2` | `web/src/lib/code-ui/sse-resilience/v2-wire.ts:23` |
| 服务端省略仍 V1 | `sse_wire.rs:43` |
| DEFER-08 清单第 2/3 条未完成 | `docs/commands/code.md:422-432` |

**Acceptance criteria:**

- [ ] 内置 control/automation 客户端请求 `wire=2`（或 Accept `libra-wire=2`）
- [ ] `code_ui_remote_sse_matrix` 与 wire 测试默认断言 v2；显式 v1 仍有兼容用例
- [ ] 文档清单第 2、3 条可勾选为证据（仍不删 v1）
- [ ] 不改变省略 `wire` 的服务端默认值

**Verification:**

- [ ] `LIBRA_ENABLE_TEST_PROVIDER=1 cargo test --features test-provider --test code_ui_remote_sse_matrix -- --test-threads=1`
- [ ] `source .env.test && cargo test --test ai_code_ui_wire_test sse_wire_version_negotiation`
- [ ] `rg -n "Built-in automation clients migrated to v2" docs/commands/code.md`（锚点定位用，非判据）

**Dependencies:** DF-01

**Deliverables:** N/A

**Implementation write set:** `src/command/code_control.rs` 或 control stdio 客户端路径；`tests/code_ui_remote_sse_matrix.rs`；`tests/ai_code_ui_wire_test.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** revert

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=automation/compat 默认 SSE v2; recovery=revert 客户端与测试默认; complete=yes; self-contained=yes; AC=4/8; VER=3/8; landing=2; prod-files=6; scope=M; deps=DF-01; writeset=序列化于 DF-06; release=independent; split-from=N/A; exception=N/A`

### Task DF-06: 服务端 SSE 省略 `wire` 时默认 v2

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 把 `DEFAULT_CODE_UI_SSE_WIRE_VERSION` 改为 `V2`；显式 `wire=1` 仍提供 snapshot stream。唯一行为轴 = 服务端默认版本。发布说明必须写出「最后默认 v1 的版本」以便 DEP-02 烘焙。

**Out of scope:** 删除 v1 分支（DF-08）；改 SPA（已是 v2）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 默认 V1 | `src/internal/ai/web/sse_wire.rs:43,85` |
| 协商矩阵已存在 | `parse_code_events_wire_version` |

**Acceptance criteria:**

- [ ] 省略 `wire` 与未带 `libra-wire` 的请求得到 v2 delta stream
- [ ] 显式 `1`/`v1` 仍为全量 snapshot
- [ ] 非法值仍 `INVALID_WIRE_VERSION`
- [ ] EN/zh `docs/commands/code.md` 与 CHANGELOG 写明默认切换与最后默认-v1 版本

**Verification:**

- [ ] `source .env.test && cargo test --test ai_code_ui_wire_test sse_wire_version_negotiation`
- [ ] `LIBRA_ENABLE_TEST_PROVIDER=1 cargo test --features test-provider --test code_ui_remote_sse_matrix -- --test-threads=1`
- [ ] `source .env.test && cargo test --test compat_matrix_alignment`

**Dependencies:** DF-05

**Deliverables:** N/A

**Implementation write set:** `src/internal/ai/web/sse_wire.rs`；`src/internal/ai/web/mod.rs`（仅当默认注释/路由文档字符串）；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`；`CHANGELOG.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** revert

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=服务端 SSE 默认 v2; recovery=revert 默认常量，显式 v1 保留; complete=yes; self-contained=yes; AC=4/8; VER=3/8; landing=2; prod-files=4; scope=S; deps=DF-05; writeset=序列化于 DF-05; release=independent; split-from=N/A; exception=N/A`

### Task DF-07: skill activate 产生 provider 可消费效果

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** `POST /api/code/skills/activate` 在 A0-07 可发现性校验通过后，把 skill 激活效果交给 in-process provider（allowed-tools 不放宽），不再固定 `SKILL_ACTIVATION_UNSUPPORTED`。未知 provider 仍 fail-closed。

**Out of scope:** 新建 skill discovery store（永久非目标，A0-07 已有）；session 热切换（DEFER-DF-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 校验通过即 `Ok(())` 无效果 | `execution_control.rs:221-239` |
| 公开契约仍 422 | `docs/commands/code.md:437,484` |

**Acceptance criteria:**

- [ ] 可发现 skill 激活后后续 turn 能消费该 skill（或等价 allowed-tools/context 收窄/注入，有测试）
- [ ] 未知 provider / 不可发现 name 仍稳定错误，不 422 `SKILL_ACTIVATION_UNSUPPORTED` 冒充「未实现」
- [ ] 不新建 skill registry
- [ ] EN/zh code 文档删除「until a provider-consumed activation path exists」

**Verification:**

- [ ] `source .env.test && cargo test --test ai_runtime_contract_test skill_search_activation_uses_a0_projection`
- [ ] `LIBRA_ENABLE_TEST_PROVIDER=1 cargo test --features test-provider --test code_ui_remote_state_matrix -- --test-threads=1`
- [ ] `source .env.test && cargo test --lib internal::ai::web::mod::tests::code_skill_activate_rejects_discoverable_skill_without_activation_effect`（本卡后该用例须改名或改为断言有效果，不得空跑）

**Dependencies:** DF-06（`docs/commands/code.md` I–I）

**Deliverables:** N/A

**Implementation write set:** `src/internal/ai/runtime/execution_control.rs`；`src/internal/ai/web/agent_runtime_adapter.rs`；`src/internal/ai/web/mod.rs`；`src/internal/ai/web/code_ui.rs`（错误表）；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`

**Release write set:** Inherited

**Files likely touched:** 同上；前端 `web/src/lib/code-ui/goal-task-skill/skill.ts` 仅当文案依赖 422

**Rollback mode:** revert

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=skill activate provider 效果; recovery=revert 激活接线，search 保持; complete=yes; self-contained=yes; AC=4/8; VER=3/8; landing=3; prod-files=6; scope=M; deps=DF-06; writeset=序列化于 DF-06; release=independent; split-from=N/A; exception=N/A`

### Task DF-08: 物理删除 SSE v1 snapshot stream

**Task type:** removal

**Lifecycle / Acceptance:** pending / 空

**Description:** 在 DEP-02 烘焙满足后删除 v1 snapshot SSE 分支与默认协商中的 v1。唯一行为轴 = 删除已弃用的公开 wire 版本。

**Out of scope:** 改 v2 容量阈值（永久非目标，属 0715 W3-08 已关闭）；把 SPA 迁回 v1（永久非目标）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| v1/v2 双分支 | `src/internal/ai/web/mod.rs` `code_events_handler` match |
| 删除前置清单 | `docs/commands/code.md:422-432` |

**Acceptance criteria:**

- [ ] DEP-02 证据已附任务记录（tag + `gh release view`）
- [ ] `CodeUiSseWireVersion::V1` 及 snapshot stream 分支删除；请求 `wire=1` 返回稳定 400
- [ ] compat / wire 测试不再消费 v1
- [ ] EN/zh 文档与 CHANGELOG 写明 breaking：只支持 v2

**Verification:**

- [ ] `source .env.test && cargo test --test ai_code_ui_wire_test sse_wire_version_negotiation`
- [ ] `LIBRA_ENABLE_TEST_PROVIDER=1 cargo test --features test-provider --test code_ui_remote_sse_matrix -- --test-threads=1`
- [ ] 使用模板零命中守卫确认生产 `src/internal/ai/web` 无 `CodeUiSseWireVersion::V1`

**Dependencies:** DF-06, DF-07, DEP-02

**Deliverables:** N/A

**Implementation write set:** `src/internal/ai/web/sse_wire.rs`；`src/internal/ai/web/mod.rs`；`tests/ai_code_ui_wire_test.rs`；`tests/code_ui_remote_sse_matrix.rs`；`docs/commands/code.md`；`docs/commands/zh-CN/code.md`；`CHANGELOG.md`；`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** immutable-release（v1 客户端只能升级；不得靠 revert 已发布 tag 恢复 v1）

**Migration and rollback:** 用户升级到本卡版本或继续使用 DEP-02 烘焙版本；降级指引写 CHANGELOG

**Estimated scope:** M

**Version increment:** minor（删除公开 wire 版本，ADR-DF-03）

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=removal; axis=物理删除 SSE v1; recovery=发新版本或指引用户留在烘焙版本; complete=yes; self-contained=yes; AC=4/8; VER=3/8; landing=2; prod-files=5; scope=M; deps=DF-06,DF-07,DEP-02; writeset=序列化于 DF-07; release=independent; split-from=N/A; exception=N/A`

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | SSE 默认协商、skill activate、commit tree 锁 | `--lib` filters 见各卡 |
| 集成 | review admission、四条执行结局、investigate CLI、JSON 错误 | `agent_review_workflow_test`、`agent_investigate_workflow_test`、`command_test` |
| 兼容 | 矩阵/错误码/版本面 | `compat_matrix_alignment`、`compat_error_codes_doc_sync`、`compat_version_surface_sync` |
| 安全 | untrusted seed 011、无 runtime 010、loopback 不放宽 | review/investigate 用例 + 既有 write_guards 回归 |
| 性能 | 省略 wire 后不再默认全量 snapshot | `code_ui_remote_sse_matrix` |
| live/gated | 无本计划强制 L3 | N/A |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| DF-01 | 0715 closeout vs tracing C10 | `docs/development/tracing`、三份 plan | plan-long 索引 | rg 过期原句零命中 |
| DF-02 | 0715 DEFER-10 | `commit.rs` 测试隔离 | 0715 DEFER-10 关闭句 | `test_create_tree` |
| DF-03 | 0715 DEFER-01 / 0714 PD-01 | `review.rs` + runtime helper admit | review EN/zh、error-codes、agent.md | `review_fix_bridge_admission` |
| DF-09 | 自 DF-03 拆出 | helper 执行结局 | review EN/zh 四结局 | `review_fix_bridge_{approval_denied,sandbox_denied,tool_failure,success_patch}` |
| DF-04 | 同上 | `investigate.rs` | investigate EN/zh、0714 PD-01 收口 | `investigate_fix_bridge` |
| DF-05 | 0715 DEFER-08 清单 2–3 | control client + sse 测试 | code EN/zh 清单 | `code_ui_remote_sse_matrix` |
| DF-06 | 0715 DEFER-08 默认切换 | `sse_wire.rs` | code EN/zh、CHANGELOG | `sse_wire_version_negotiation` |
| DF-07 | code.md skill 422 残差 | `execution_control.rs` + web handler | code EN/zh | skill activate 用例 |
| DF-08 | 0715 DEFER-08 物理移除 | 删除 v1 分支 | COMPATIBILITY、CHANGELOG | wire 测试 + V1 零命中 |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | DF-01 | no-release 文档 | revert 文档 |
| M1 | DF-02/03/09/04 | 各卡 patch + gh release | revert 各卡；fix 回到 010 |
| M2 | DF-05/06/07 | 各卡 patch + gh release | revert 默认 v2 / skill 效果 |
| M3 | DEP-02 + DF-08 | DF-08 minor + gh release | immutable-release：新版本或停留烘焙版 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| DF-03 半接入（CLI 已进 queue 但无 hardening） | 无 | 立即 revert；保持 010 | 无审批 mutation |
| DF-06 已发布、automation 未迁 | 显式 v1 仍可用 | 前滚 DF-05 或文档紧急提示 `wire=2` | 删除 v1 |
| DF-08 已发布、旧客户端仍要 v1 | 400 | 指引升级或使用烘焙版本 | 偷偷加回 v1 当默认 |
| DF-02 锁隔离失败 | 测试红 | FIX 卡，不改生产 commit | 关掉测试冒充全绿 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 0714 与本计划双实现 fix bridge | 高：双 queue | DEP-01；DF-03 开工前 `rg` 无第二入口 | DF-03/09/04 |
| 默认切 v2 但 automation 仍吃 v1 | 中：大 session 卡顿 | DF-05 先于 DF-06 | DF-05/06 |
| untrusted seed 漏 011 | 高：不可信 findings 改树 | AC 强制 011 先于 mutation | DF-03 |
| 满载测试锁误杀 C 组 | 中：任意卡假红 | DF-02 尽早发布 | DF-02 |
| 热切换 session 偷渡进 DF-07 | 中：lease 竞态 | ADR-DF-04 | DEFER-DF-01 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| SSE 省略 wire（DF-06 后） | v2 delta | 受 1024 条 / 8 MiB 传输帽 | 同 0715 GC-CODE-12 | `code_ui_remote_sse_matrix` |
| review --fix | 一次 serialized turn | 不并行第二 queue | 与 Code session 相同 hardening | `review_fix_bridge` |
| test_create_tree | 单测 tree 构建 | 不得阻塞全套件 | 无全局锁等待超时 | 满载 `cargo test --all` |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md`：DF-08 同步 wire v1 删除；其它卡若无公开 Git 面则 N/A
- [ ] `docs/commands/review.md` / `investigate.md` / `code.md` 及 zh-CN：随各卡
- [ ] `docs/development/commands/`：仅当开发契约漂移时
- [ ] `docs/error-codes.md`：010/011 语义从「未落地」改为「无 runtime / untrusted seed」
- [ ] `tests/INDEX.md`：仅新增/重命名 target 时
- [ ] `Cargo.toml` `[[test]]`：本计划不预期新 compat 文件，N/A 除非 DF-03 新增 target
- [x] `plan-long.md`：DF-01 增加本计划索引行

## Codex review log

本计划强制通道是 Claude（ER-DF-01）。本表保留给可选 Codex 交叉复审。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| — | 未跑 | — | — | — | — |

## Claude review log

Result 只允许 `PASS` 或 `FAIL`。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| Claude-R1 | 本文件对照 `plan-template.md` v2.1 与当前源码 | FAIL | 2×P0（DF-03/DF-04 AC 四结局压条超 G-03）；1×P1（实施顺序漏 DEP-03/DEP-01） | 2×P2 已顺手修（DEP-02 Owner、版本面 0.21.22） | 本会话 `claude -p --permission-mode plan`；log `terminal/call-0152ce6b-19f9-4b7b-9160-1a0f17769474-117.log` |
| Claude-R2 | R1 修订后复审 | FAIL | 1×P0（DF-07/DF-08 I–I 无边） | P2 crate 行 0.21.21 未改完 | 本会话 `claude -p --permission-mode plan`；log `terminal/call-d634e08c-6abc-454e-9c89-dc24826e21e6-145.log` |
| Claude-R3 | R2 修订后复审 | FAIL | 1×P0（DEP-01 方向反了：outgoing 产物被写成 DF-04 前置，自指环） | 无新 P2 | 本会话 `claude -p --permission-mode plan`；log `terminal/call-e240580f-509b-4413-af10-6e33acc9a34e-158.log` |
| Claude-R4 | R3 修订后复审 | PASS | 0×P0 / 0×P1；R1–R3 全部 CLOSED | 1×P2 residual（DEP-01「超时与失败策略」列写的是抢先双实现场景，而非模板「接收方未接手」字面；不阻断） | 本会话 `claude -p --permission-mode plan`；log `terminal/call-fb9b6fe7-d386-44aa-b80b-412a733059a9-168.log` |
| Claude-DF01-R1 | DF-01 四份交付物、Lifecycle/Acceptance 与验证记录 | PASS | 0×P0 / 0×P1 | 初审发现日期计划索引排序 P2，已移至 20260822 后；闭环复审无残留 P2 | 本会话本地 Claude Code 2.1.240，`claude -p --permission-mode dontAsk` 只读审计；结果保留于当前 Goal transcript |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-DF-01 | in-process AgentRuntime session 热切换 | 独立 lease/durability 轴 | 设计通过 Claude/Codex review | 后续日期计划 |
| DEFER-02 | 独立 `libra mcp --stdio` | 0715 原 DEFER；本计划不扩 MCP 命令面 | W5 已过，可另立计划 | 新日期计划 |
| DEFER-03 | MCP 授权门闭环 | 生产 `authz=None` allow-all；Web 不依赖它 | MCP authz 单独立项 | 新日期计划 |
| DEFER-04 | 非 loopback 远程写面 | 安全姿态：SSH port-forward 是正式路径 | authenticated TLS 设计立项 | 新 RFC/ADR |
| DEFER-05 | `libra code -control` | clap 不可自然兼容 | 不重启 | 0715 产品决策 |
| DEFER-07 | Codex user-input 完全纳管 | app-server 协议未提供可替换本地回环的稳定面 | 0715 DEFER-07 三条重启条件 | W3-07 边界文档 |
| DEFER-09 | （CLOSED）WIO 架构移交 | WIO-01..03 已完成 | N/A | 0715 Phase IO |

0715 DEFER-01/08/10 由本计划 DF-03/04、DF-05/06/08、DF-02 承接，不再作为本计划 DEFER 行。

## 完成判据

- [ ] 粒度审计表 9 张卡均达标；无 L/XL、无未登记合并发布、无 I–I 未消解冲突
- [ ] 非延后任务（DF-01..DF-09）均为 `Lifecycle=done` 且 `Acceptance=complete`；DF-08 不得在 DEP-02 未满足时 complete
- [ ] 各卡 Verification 已运行并记录；`(new)` 用例真实执行
- [ ] 计划完成门：`cargo +nightly fmt --all --check`；`cargo clippy --all-targets --all-features -- -D warnings`；`source .env.test && cargo test --all`；若改 `web/` 则 `pnpm --dir web lint/test/build`
- [ ] 文档/compat/error-codes/INDEX 按「兼容与文档收口」完成
- [ ] Claude review 最终 `PASS`，P0/P1 关闭
- [ ] 会发版卡均 `patch + 1`（DF-08 为 minor）、五处版本面一致、`gh release create` 已触发
- [ ] 修订历史记录成稿后规范性变更
- [ ] `plan-long.md` 已索引本计划
