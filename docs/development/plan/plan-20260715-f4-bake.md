# plan-20260715 F4 bake evidence

Web-default bake gate (ADR-CODE-08 / Phase 5 entry): **3 successful public patch releases**, **≥7 calendar days** from W4-01, **three independent smokes**, **no P0/P1 Web-default regressions**.

## Calendar

| Milestone | Date (UTC) | Notes |
|-----------|------------|-------|
| W4-01 Web-default ship | 2026-08-12 | `74a95b3` / **v0.19.142** |
| Evidence refresh | 2026-08-15 | day **3 / 7** |
| Earliest W5-01 family open | **2026-08-19** | ≥7 natural days from W4-01 |

## Public patch releases (post W4-01, pushed to `origin` / `origin-https`)

Counted only after successful push (local bump alone does not count). Non-exhaustive sample of Web-default era patches on `main`:

| Version | Card / theme | Date |
|---------|--------------|------|
| v0.19.142 | W4-01 Web default | 2026-08-12 |
| v0.19.143 | W4-02 `--control stdio` | 2026-08-13 |
| v0.19.154 | W4-05 docs closeout | 2026-08-13 |
| v0.19.155 | W6-03 install CDN | 2026-08-13 |
| v0.19.156 | WIO-01 I/O worker | 2026-08-13 |
| v0.19.157 | WIO-02 fd/beneath | 2026-08-14 |
| v0.19.158 | WIO-03 ObjectReadBudget | 2026-08-14 |

**Gate:** ≥3 public patches — **PASS** (many more than three).

## Independent smokes (2026-08-15)

### Smoke 1 — W4-01 manual fake-fixture Web default

Command (debug binary built with `--features test-provider`):

```bash
LIBRA_ENABLE_TEST_PROVIDER=1 LIBRA_TEST=1 \
  target/debug/libra code --provider fake \
  --fake-fixture tests/fixtures/code_ui/basic_chat.json \
  --browser-control loopback
```

Result (headless, then SIGTERM):

- `Libra Code server running at http://127.0.0.1:3000?bt=…`
- Browser bootstrap token printed
- MCP `http://127.0.0.1:6789`
- Clean stop on SIGTERM
- Log dir: `/tmp/f4-smoke1-gqpYfh/`

### Smoke 2 — deterministic Code UI scenario

```bash
LIBRA_ENABLE_TEST_PROVIDER=1 cargo test --features test-provider \
  --test code_ui_scenarios basic_chat_submit_updates_transcript \
  -- --test-threads=1
```

Result: `ok` (1 passed). Log: `/tmp/f4-smoke2-code_ui_scenarios.log`.

### Smoke 3 — CLI status + graph help surface

```bash
libra --json status
libra graph --help   # documents Web Code UI; interactive TUI deprecated
libra graph --json   # usage error LBR-CLI-002, no hang
```

Result: status JSON `ok: true`; graph help shows deprecation; missing UUID fails closed. Log dir: `/tmp/f4-smoke3-Zy8AEa/`.

**Gate:** three independent smokes — **PASS** (archived 2026-08-15).

## Regressions

No known P0/P1 Web-default rollback between W4-01 and this evidence refresh.

## Remaining before W5-01 family

- [ ] Calendar ≥7 days (open **2026-08-19**)
- [ ] Re-confirm no P0/P1 Web-default rollback on open day
- Then start W5-01 → W5-07 → W5-06 → W5-08 (local no-push window) → W5-09 breaking publish
